# Enchentes do Vale do Itajaí

Site com dados históricos de enchentes nos rios **Itajaí-Açu** e **Itajaí-Mirim** (SC): nível do rio em cada cidade, previsão empírica para a cidade a jusante e tempo estimado de chegada da cheia. Itajaí, na foz, recebe os dois rios e a influência da maré.

**No ar:** https://haohmarusc-glitch.github.io/enchentes-vale-itajai

> Este projeto **não substitui** o AlertaBlu, a Defesa Civil de SC nem as Defesas Civis municipais. Em emergência, ligue 199.

## Estrutura

```
data/
  estacoes.json   cidades, códigos ANA, cotas de referência, ordem no rio
  enchentes.json  picos históricos por rio/cidade/data, com fonte e confiança
  transito.json   tempo que a cheia leva para descer entre cidades
  mare-itajai.json tábua de maré do porto de Itajaí (preamares e baixa-mares)
  tempo-real/      série coletada dos níveis (fora do git; só ultimo.json entra)
scripts/
  validar_dados.py     portão de qualidade dos JSONs (roda no CI)
  publicar_tempo_real.sh publica a última leitura no branch `tempo-real`
  auditar.py           audita a coleta e compara a defasagem observada com a publicada
  coleta_mares.py      baixa a tábua de maré da Defesa Civil de Itajaí
  coleta_niveis.py     coleta contínua dos níveis, acumulando a série de uma cheia
  coleta_itajai.py     leitura avulsa dos níveis (mostra e sai)
  extrair_picos.py     acha os picos na série e PROPÕE registros para enchentes.json
  ana_hidroweb.py      coleta de séries da ANA (aguarda credenciais)
  calibrar_transito.py mede o tempo real de descida a partir dos horários de pico
web/                   site em React + Vite + TypeScript
```

Os JSONs de `data/` são a **fonte de verdade**. O site lê deles; os scripts escrevem neles.

## Rodar

```bash
cd web
npm install
npm run dev      # desenvolvimento
npm test         # testes da lógica de previsão e de trânsito
npm run build    # build estático em web/dist (zero erro de tipo)
```

```bash
cd scripts
python3 -m pip install -r requirements.txt
python3 validar_dados.py            # sempre antes de commitar mudança em data/
python3 teste_coleta_mares.py       # testes do analisador da tábua de maré
python3 coleta_mares.py --verificar # mostra a tábua lida do endpoint, sem gravar
python3 coleta_mares.py             # grava data/mare-itajai.json
python3 coleta_niveis.py            # coleta e acumula a série dos níveis
python3 extrair_picos.py            # propõe registros de pico a partir da série
python3 calibrar_transito.py        # relata o que dá para calibrar
python3 auditar.py                  # audita os últimos 15 dias de coleta
python3 conferir_saltos_serie.py    # acusa régua misturada na série publicada
python3 conferir_par_regua.py rio-do-sul   # a cota aplicada é da régua que o site lê?
python3 medir_mare.py               # quais réguas podem disparar sozinhas
```

### Testar no navegador

Os testes automatizados não veem a tela: tile que não carrega, canvas que não
desenha, animação que não roda, texto que não cabe no celular e marca d'água de
provedor passam verdes por eles. O que fecha essa lacuna é
**`docs/testes-navegador.md`** — 19 testes com critério de passa/falha, escrito
para ser entregue **inteiro** a alguém (ou a um agente) com um navegador, sem
explicação junto.

Rodar depois de toda mudança no MAPA, na SÉRIE ou no fundo de tiles. Dois
defeitos de 04/09/2026 nasceram justamente onde os 281 testes não alcançam: o
pino de Itajaí dizendo "sem régua" numa cidade com onze, e o CARTO passando a
cobrir o mapa com "API KEY REQUIRED".

O arquivo abre com um **PASSO 0** que confere se a coleta na VPS já publica a
régua de cada ponto (`"reguas"` em `serie-recente.json`) — sem isso, dois dos
testes medem o dado antigo e não valem.

### Auditar

`auditar.py` responde três perguntas sobre a série já coletada:

1. **A coleta está viva?** Estação que parou de publicar, buraco na série, sensor travado
   repetindo o mesmo valor. Descobrir na cheia que uma régua está muda há três dias é tarde.
2. **As leituras são plausíveis?** Valor fora de faixa, salto impossível.
3. **A defasagem observada bate com a publicada?** Por correlação cruzada entre as duas pontas
   de cada trecho de `transito.json`.

Sobre a terceira, o limite que não dá para contornar: **onda de cheia viaja mais rápido que
água baixa**. Uma defasagem medida em período seco não confirma nem refuta a faixa da JICA,
que vale para cheia — serve para pegar erro grosseiro (trecho invertido, cidade trocada, faixa
fora de ordem de grandeza). O relatório repete isso, para ninguém ler o resultado como
validação do que não foi validado.

Ele sai com código 1 quando há problema de **coleta**, que é operacional e precisa acordar
alguém. Defasagem fora da faixa em água baixa é observação, não erro, e não derruba o código
de saída. No cron, uma vez por dia já basta.

### Registrar uma cheia nova

O que falta em quase todo registro de `enchentes.json` é o **horário do pico** — e é
ele que troca o hidrograma de projeto da JICA por medição de cheia real. O caminho:

1. `coleta_niveis.py` no cron (`*/15 * * * *`) acumula uma linha por medição em
   `data/tempo-real/AAAA-MM.ndjson`. Deduplica pelo carimbo da fonte, então rodar
   com mais frequência não infla o arquivo: a página atualiza a cada 15–30 min.
   São ~110 bytes por leitura, algo como 40 MB por ano; `--compactar` reduz os meses
   fechados a cerca de um décimo. Esses arquivos ficam **fora do git**.
2. Passada a cheia, `extrair_picos.py` lê a série **régua por régua** — nunca juntando
   estações da mesma cidade, que têm zeros diferentes — separa os episódios acima da cota
   de atenção e imprime os registros propostos, com data e hora.
   Ele **não grava**: confira cada pico contra o boletim da Defesa Civil antes.
3. Conferido, `extrair_picos.py --escrever` inclui os registros, e
   `calibrar_transito.py` passa a ter material para medir os tempos de descida.

Isso só rende a partir da **próxima** cheia: a página publica o nível de agora, não
o histórico. Para as antigas, o caminho continua sendo os boletins da Defesa Civil e
o acervo do CEOPS.

Para manter a maré em dia, `coleta_mares.py` deve rodar de tempos em tempos (cron numa
máquina qualquer) e o `data/mare-itajai.json` resultante ser commitado. Enquanto o arquivo
estiver vazio, a tela da foz pede a tábua a quem estiver usando — ela não estima horário
de preamar.

O site é estático e usa `HashRouter`, então funciona em GitHub Pages ou Vercel sem reescrita de
rotas no servidor. `.github/workflows/pages.yml` publica a cada push no `main` — e **valida os
JSONs antes de construir**: se os dados não passarem, nada vai ao ar e o site no ar continua o de
antes. As publicações de tempo real vão para o branch `tempo-real` e não disparam build; a página
busca aquele branch em tempo de execução.

O **site** publica sozinho no push ao `main` (acima). Os **coletores e o bot rodam na VPS**, de
`/opt/enchentes-vale-itajai`, e o deploy de lá é um comando (como root, que tem o alias de deploy):
`scripts/deploy.sh` — traz o `main` e reinicia o bot só se o código dele (`bot.py` ou os módulos que
ele importa) mudou; os coletores pegam o código novo no próximo ciclo do cron, sem restart.

## Telas

1. **`/acu` — Itajaí-Açu** — árvore: cabeceiras Taió ‖ Ituporanga → tronco Rio do Sul → Ascurra → Indaial → Blumenau → Gaspar → Ilhota → Itajaí (Ibirama é afluente lateral, Rio Hercílio). Ver `docs/TOPOLOGIA-CANONICA.md`.
2. **`/mirim` — Itajaí-Mirim** — Vidal Ramos → Botuverá → Brusque → Itajaí
3. **`/itajai` — Itajaí (foz)** — chegada dos dois picos + maré
4. **`/`** — escolha do rio e aviso legal

## O que os dados sustentam — e o que não sustentam

**Tempo de chegada: sim.** O estudo JICA (Preparatory Survey, 2011) dá o eixo do Itajaí-Açu
inteiro: Rio do Sul → Blumenau em 7 a 10 h, Blumenau → Itajaí em 14 a 17 h, e a tabela do
hidrograma de projeto fecha os trechos intermediários (Blumenau → Gaspar 2 h, Gaspar →
Ilhota 5 h, Ilhota → Itajaí 10 h). O pico na foz cai no dia seguinte ao pico em Rio do Sul.

**Altura a jusante: não.** Com 5 eventos pareados entre Rio do Sul e Blumenau, a correlação
dá r² = 0,21 — e o site se recusa a exibir número. Não é falta de dado, é a bacia: em agosto
de 1984, 12,80 m em Rio do Sul viraram 15,46 m em Blumenau; em novembro de 2023, 13,04 m
viraram 9,14 m. O que decide o nível lá embaixo é onde a chuva caiu, não o nível lá em cima.
A tela mostra a nuvem de pontos para que isso fique visível, em vez de pedir fé no r².

**Fontes de tempo real: pelo endpoint, não pelo HTML.** A tábua de maré vem de
`ajax/mares.php`, o mesmo JSON que o gráfico do site consome — `tidelevel` em centímetros
na série observada, `level` em metros na astronômica (a chave `astronimical_tides` tem o
erro de digitação na própria API). Os níveis dos rios vêm do HTML da página de níveis, cuja
estrutura está documentada em `coleta_itajai.py` e coberta por testes com o markup real.

**Nível ao vivo: com a idade sempre à vista.** O site busca a última leitura em tempo de
execução (não no build, que teria a idade do último deploy). A idade aparece junto do número;
passando de 3 h a tela diz, em letras, que aquilo não serve como nível atual. O cálculo de
chegada a jusante só roda com leitura de até 45 minutos — com dado velho os horários sairiam
já vencidos, com cara de previsão. E é sempre condicional: **se** o pico for agora, porque o
tempo de descida é medido de pico a pico e o rio pode subir por mais horas.

Cidade com mais de uma régua não mostra nível ao vivo. Itajaí tem cinco só no Mirim, com
zeros diferentes — escolher uma e chamar de "o nível de Itajaí" seria comparar réguas.

**Maré em Itajaí: qualitativa, nunca em metros.** A preamar não soma centímetros — ela trava
o escoamento. O site cruza a janela de chegada com as preamares da tábua oficial e diz se a
cheia chega na maré alta, e se é período de sizígia (calculado da fase da lua, que é exata).
Não converte isso em altura: não há nada nos dados do projeto que calibre esse número.

## Como o site trata dado incerto

O projeto pode custar vidas se errar, então a regra é preferir dizer "não sei":

- **Sem 5 eventos pareados, não há previsão.** A tela mostra "dados insuficientes" e diz quantos faltam.
- **Leituras da mesma cidade a até 7 dias uma da outra são a mesma cheia**, e o pico do evento é a maior delas. Sem isso, 8 e 9 de setembro de 2011 em Blumenau contariam como dois eventos. Quando um registro de mês inteiro casa com duas cheias distintas, o par é descartado em vez de o código escolher uma.
- **Valores divergentes ficam guardados, não apagados.** Setembro de 2011 em Blumenau tem três leituras publicadas — 12,60 m (imprensa), 12,80 m (série municipal) e 13,00 m (CEOPS, Ponte Adolfo Konder). O arquivo adota uma e mostra as outras em `divergencias`, com a fonte de cada uma.
- **Previsão não vira histórico.** A cota de 9,10 m divulgada para Brusque em 17/11/2023 era previsão de pico; o registro guarda a medição (8,96 m) e a previsão como divergência.
- **Correlação fraca (r² < 0,50) também não vira número.** Nem relação decrescente, que indicaria erro de pareamento ou de régua.
- **A estimativa é sempre uma faixa** (intervalo de previsão de 95%), nunca um valor único. Se o nível informado estiver fora do que já se observou, a tela avisa que está extrapolando.
- **Tempo de trânsito é sempre faixa** ("14–17 h"). Quando a fonte traz um valor único, a tela diz "por volta de" e explica que é aproximação grosseira.
- **Cada cidade tem sua própria régua.** O aviso está em todas as telas, e o gráfico nunca põe duas cidades no mesmo eixo.
- **Todo número mostra a fonte e o grau de confiança**; registro sem fonte é descartado na leitura.
- **Registros duplicados para o mesmo evento são descartados** no pareamento, em vez de o código escolher um.

Essas regras estão em `web/src/logica/previsao.ts` e cobertas por testes em `web/src/logica/*.test.ts`.

## Fontes

- ANA / HidroWeb (séries históricas): https://www.snirh.gov.br/hidroweb
- Defesa Civil SC (tempo real): https://monitoramento.defesacivil.sc.gov.br/
- AlertaBlu (Blumenau): https://alertablu.blumenau.sc.gov.br/
- Defesa Civil de Itajaí: https://defesacivil.itajai.sc.gov.br/monitoramento/nivel-rios
- CEOPS/FURB (acervo histórico): http://ceops.furb.br/

## Avisos (Telegram)

O site é *pull*: só serve para quem o abre. Ninguém abre uma página às três da
manhã, que é quando várias das cheias do Vale atingiram o pico. Estes dois
scripts vão atrás da pessoa.

```bash
cp .env.example .env          # preencher TELEGRAM_BOT_TOKEN e TELEGRAM_CHAT_ID
python3 scripts/notificador.py --teste
python3 scripts/alerta_cotas.py --seco    # mostra o que enviaria, sem enviar
python3 scripts/saude_coleta.py           # sai 0 se a coleta está viva, 1 se não
```

No cron, logo depois da coleta:

```cron
*/15 * * * * cd /root/enchentes-vale-itajai && python3 scripts/coleta_niveis.py && python3 scripts/alerta_cotas.py
17 * * * *   cd /root/enchentes-vale-itajai && python3 scripts/saude_coleta.py --avisar
```

### As regras de aviso, e por que não são as de um bot comum

**Não há horário de silêncio.** Um bot de notificação normal cala de madrugada.
Aqui isso mataria gente: 2008 e 2011 subiram de noite, e o aviso da madrugada é
o único que dá tempo de sair de casa. Quem quiser silêncio configura no próprio
Telegram — não no código de todo mundo.

**O intervalo entre avisos não é cronômetro.** Repetir "está em alerta" a cada
45 min vira ruído, e ruído faz a pessoa desligar o bot justamente antes da noite
em que ele importaria. Então: mudança de faixa avisa sempre; a mesma faixa só
repete depois de 3 h **e** se o rio tiver subido 30 cm desde o último aviso; a
descida e a volta ao normal avisam uma vez.

**Cada régua tem sua cota.** O limiar vem da própria estação. A cota da cidade
só vale onde a cidade tem uma régua só — em Itajaí são onze, com zeros
diferentes. Sem cota por régua, o script **não avisa** aquela estação e diz por
quê. Alarme com cota errada é pior que alarme nenhum: ensina a ignorar o
próximo.

**O aviso não calcula chegada a jusante.** O encadeamento de tempo de descida
vive em TypeScript (`web/src/logica/transito.ts`), e reescrevê-lo em Python
criaria duas contas de vida diferentes que podem divergir em silêncio. A
mensagem manda o link do site, onde a conta é uma só.

### O vigia da coleta

A coleta também se confere sozinha: se uma estação veio na rodada anterior e não
veio agora, `coleta_niveis.py` nomeia as que sumiram no `stderr` e grava a lista
em `estacoes_ausentes` no `ultimo.json`. O aviso **não** muda o código de saída,
de propósito — o cron encadeia `coleta_niveis.py && publicar_tempo_real.sh`, e
sair com erro faria o site congelar no dado anterior em vez de receber as
leituras que chegaram. Isso importa porque o vigia roda de hora em hora enquanto
a coleta roda a cada quinze minutos: sem esta conferência, três de cada quatro
coletas nunca seriam olhadas.

`saude_coleta.py` responde a duas perguntas, e a segunda é a traiçoeira:

1. **a coleta rodou?** (`coletado_em`) — cron morto, disco cheio, pacote quebrado;
2. **a fonte publicou?** (`medido_em` mais recente) — o cron correndo
   perfeitamente a cada 15 min sobre uma página que parou de atualizar há seis
   horas. O arquivo fica novo; o dado, velho.

Falha avisa na hora e não repete antes de 6 h. A recuperação avisa sem esperar.

## Chuva acumulada

Ao lado do nível, cada cidade mostra quanto choveu, do pluviômetro da própria
cidade. Coletado por `scripts/coleta_chuva.py` da segunda página da mesma
fonte (`/monitoramento/chuvas`) e publicado junto do nível em `ultimo.json`.

**As janelas são as da fonte: 10 min, 1 h, 12 h, 24 h e 48 h.** Ela não publica
6 h, e aqui não se estima — numa cheia a chuva não cai constante, e dividir o
acumulado de 12 h suporia justamente o contrário. A metade final de um período
de 12 h pode conter toda a chuva.

**É pluviômetro, não radar.** Radar mede refletividade e estima intensidade
sobre uma área; não vira milímetro acumulado confiável num ponto. Um número de
radar ao lado do nome da cidade teria cara de medição sem ser uma.

**Milímetro se compara entre cidades; metro de régua não.** Por isso a chuva de
uma cidade com vários pluviômetros é agregada e o nível não: cinco aparelhos em
Itajaí medem a mesma grandeza. Mostra-se o **maior**, não a média — o que enche
o rio é a chuva onde ela caiu, e a média entre um ponto encharcado e um seco
inventa um meio-termo que não aconteceu em lugar nenhum. Quando os pontos
discordam, a faixa inteira aparece: em 30/08/2026, Itajaí marcava
`14,0–39,6 mm em 24 h` entre seis pluviômetros.

**Trava de coerência.** As janelas são encaixadas, então o acumulado tem de ser
não-decrescente. A fonte publica série que viola isso: a estação Guarani, em
Brusque, registrava 0,20 mm nos últimos 10 minutos e 0,00 mm em 1 h, 12 h, 24 h
e 48 h no mesmo instante. Zero ali quase certamente é "sem dado", e mostrá-lo ao
lado de uma vizinha com 39 mm mandaria a pessoa para o lado errado. A leitura vai
marcada e a tela diz **"dado inconsistente na fonte"** em vez de um número.

### A coleta insiste quando vale a pena

Toda coleta passa por `comum.baixar()`. Antes cada script fazia `requests.get`
seco: um soluço de rede num servidor municipal — que é o que estas fontes são —
derrubava a coleta inteira e, no cron encadeado com `&&`, derrubava junto a
publicação. Quinze minutos sem número novo no site por causa de um TCP reset.

A ideia veio do Fila-Disney, e o motivo dele vale ainda mais aqui: *um ciclo
perdido é histórico perdido para sempre*. Numa cheia o ciclo perdido pode ser
justamente o do pico — o dado que depois faltaria para calibrar o tempo de
descida da próxima.

Insiste em 5xx e timeout, com espera crescente. **Não** insiste em 4xx que não
seja 429 (página que mudou de endereço não volta na segunda tentativa) nem em
rede fora (DNS quebrado não melhora esperando). Em 429 respeita o `Retry-After`
que o servidor mandou — ignorar isso é o caminho para levar bloqueio de uma
fonte pública que usamos de graça.

## Bot de consulta no Telegram

O `alerta_cotas.py` fala sozinho quando um rio cruza cota. O `bot.py` é o
contrário: responde quando a pessoa pergunta — de madrugada, sem abrir o site,
com internet ruim. Uma mensagem de texto passa onde uma página não passa.

| Comando | O que devolve |
|---|---|
| `/rua <cidade> <rua>` | a partir de quantos metros aquela rua alaga, e quanto falta o rio subir |
| `/nivel <cidade>` | nível agora, estação e idade da leitura |
| `/chuva <cidade>` | acumulado de 1 h, 12 h, 24 h e 48 h |
| `/previsao <cidade>` | se o pico fosse agora, quando chega em cada cidade a jusante |
| `/cotas <cidade>` | cotas de referência daquela régua |
| `/rios` | panorama de tudo que está sendo medido |
| `/emergencia` | telefones e fontes oficiais |
| `/ajuda` | a lista |

```bash
sudo cp deploy/enchentes-bot.service /etc/systemd/system/
sudo systemctl daemon-reload && sudo systemctl enable --now enchentes-bot
journalctl -u enchentes-bot -f
```

**Três regras em toda resposta:** nunca inventa número (sem dado, diz que não
tem); toda leitura sai com a idade; toda resposta lembra que isto não é alerta
oficial e que emergência é 199.

**Qualquer pessoa pode perguntar.** Diferente do bot do Fila-Disney, que
restringe comandos a um chat, aqui o público é a razão de existir — e tudo que
os comandos devolvem já é público. O que continua restrito é o *aviso
automático* de cota, que vai só para o `TELEGRAM_CHAT_ID`.

### O gabarito de trânsito

`/previsao` precisa do mesmo encadeamento de tempos de descida que o site faz —
e o site é TypeScript, o bot é Python. Duas implementações de uma conta de vida
podem divergir em silêncio: o site dizendo uma coisa, o bot outra, sem ninguém
perceber até a noite errada.

O que impede isso é `data/transito-esperado.json`: o resultado de **todo par de
cidades**, gerado a partir do `transito.ts` do próprio site. Os dois lados têm
teste que o reproduz, e a CI roda os dois.

```bash
cd web && npm run gabarito     # depois de mexer em transito.json ou transito.ts
git diff data/transito-esperado.json   # e CONFERIR: o gabarito só vale se alguém olhou
```

Se um dos testes ficar vermelho, não ajuste o gabarito para calar o teste: ou
uma implementação divergiu da outra, ou alguém mudou a lógica sem regerar. Nos
dois casos a pergunta é qual das duas está certa.

## Cotas de rua e manchas de inundação

O dado mais direto do projeto: **a partir de que nível do rio cada rua começa a
alagar**. Não passa por modelo nenhum — é leitura de tabela. E responde a
pergunta que a pessoa realmente faz, que nunca foi "quantos metros" e sim
"a minha rua".

`data/cotas-ruas.json` — 4.592 registros de Blumenau, Gaspar, Rio do Sul e Brusque,
cada um com fonte, data e confiança — e, em Blumenau, com o **abrigo** daquele
ponto. Regras que o validador e os testes travam:

- **O registro é por PONTO, não por rua.** A Rua São Rafael, em Blumenau, alaga
  a 7,40 m no final e a 7,75 m perto do nº 169. Deduplicar por nome perderia a
  cota mais baixa, que é a que importa.
- **`cota_m` nulo é resposta legítima** — a fonte cita a rua e não publica o
  número — mas exige uma nota dizendo isso. Sem a nota vira buraco silencioso,
  e alguém depois preenche com um chute.
- **O aviso não pode chegar depois da água.** Se a cota mais baixa cadastrada
  para a cidade for maior que a da primeira rua, o validador dá ERRO. Foi assim
  que descobrimos que o aviso de Brusque disparava 1,20 m tarde demais — e, na
  importação de 31/08/2026, que há um ponto da Av. Beira Rio (Limoeiro) alagando
  a **3,76 m**, 1,04 m antes da cota de atenção da cidade.
- **Número verdadeiro entra; o que ele move é outra conversa.** Esse ponto de
  3,76 m aparece na tela e no bot com a ressalva, mas leva `usar_para_aviso:
  false`: baixar o limiar da cidade inteira por causa de um ponto faria o aviso
  tocar em dia de sol, e aviso que toca à toa é desligado antes da noite em que
  importaria. Quem fecha essa lacuna é a Defesa Civil, pelo ofício C1.

`data/manchas/` — as áreas atingidas em nove eventos entre 1983 e 2015, em
GeoJSON, publicadas pela **própria prefeitura de Itajaí** na organização
GeoItajaí do GitHub, sob licença MIT. Dado oficial e aberto, o oposto do resto
que este projeto raspa de HTML. Os arquivos `inundaMÊSAAAA` trazem a
profundidade da lâmina d'água por trecho.

```bash
python3 scripts/baixar_manchas_itajai.py
```

**A mancha não promete nível de rio.** Os polígonos não trazem cota; a ligação
com o pico é feita pela data, cruzando com `enchentes.json`, e só aparece
quando aquele pico está registrado. Inventar essa ligação faria alguém olhar o
mapa de 2011 e concluir que a sua rua alaga a tal metro.

## Mapa das manchas de inundação

Na tela de Itajaí, um mapa Leaflet com as áreas atingidas em nove enchentes
entre 1983 e 2015 — dado da **própria prefeitura**, na organização GeoItajaí do
GitHub, licença MIT. Os arquivos `inundaMÊSAAAA` trazem a profundidade da
lâmina d'água por trecho, e a cor vai do azul claro ao escuro conforme a água
fica mais funda.

O **fundo do mapa** tem três opções — **Escuro** (padrão), **Satélite** e
**Mapa** —, e a escolha fica salva no aparelho. O padrão é escuro por função,
não por estética: fundo com textura concorre com a mancha, que é o dado da
tela. O satélite ajuda a reconhecer a barra, os molhes e o bairro; o mapa de
ruas ajuda a achar o endereço. Sobre satélite o contorno da mancha engrossa e
escurece, para a água não sumir contra telhado e mata. A atribuição troca junto
com a camada — é condição de licença das três fontes, não cortesia. Decisão e
comparação em `docs/CAMADAS-DE-MAPA.md`.

Três coisas que o mapa diz, e uma que ele se recusa a dizer:

- **Não é previsão.** É onde a água chegou naquele evento, na cidade que
  existia naquele ano — aterro, drenagem e construção mudaram o terreno.
- **Não estar na mancha não quer dizer que não alaga**: o levantamento cobre o
  que foi mapeado.
- Quando a fonte publica **faixas de profundidade que se sobrepõem** (out/2015
  tem "0,41 a 0,60" e "0,51 a 1"), a tela diz isso em vez de corrigir por conta.
- **Não diz com quantos metros de rio aquilo aconteceu.** Os polígonos não
  trazem cota, e o pico de Itajaí dessas datas ainda não está levantado. Sem
  isso o mapa não se compara com o nível de hoje — e essa é hoje a pendência
  mais valiosa do projeto.

O Leaflet carrega à parte, como o gráfico de picos: ele pesa mais que todo o
resto do site somado, e o mapa existe só nesta tela. Quem abre o site no celular
durante a chuva para ver o nível do rio não paga por ele.

## Cotas de Itajaí e a maré

As onze estações de Itajaí ganharam **cota oficial** de atenção, alerta e
emergência — Tabela 11 do Plano de Contingência da COMPDEC, versão 17 de
22/12/2025. A palavra "emergência" é da fonte e ficou como está: lá é a terceira
subfase, não sinônimo de inundação.

Cada uma tem seu próprio zero. A **DC-10**, no Limoeiro, usa régua de 8 a 10 m e
não se compara com as demais.

### Por que nove delas não disparam aviso sozinhas

Itajaí fica na foz. Nas réguas do estuário o nível sobe e desce com a maré duas
vezes por dia, e essa oscilação é **maior que a distância até a cota**. Medido
nos nossos próprios dados: em 30/08/2026 a **DC-01 marcou 1,24 m às 17:21** —
acima da sua cota de atenção, 1,16 m — e **0,70 m três horas depois**, sem
enchente nenhuma.

A subfase do Plano de Contingência é um estado que a Defesa Civil **declara**
olhando maré, chuva e montante juntos. Automatizar a travessia de uma régua só
reproduz o número sem o julgamento — e um aviso que toca com a maré ensina a
pessoa a ignorar o que tocar na noite da cheia.

Então: **a cota aparece na tela** para as onze, e o **aviso automático** vale
para as duas que não são de estuário (DC-10 e DC-11), mais Rio do Sul e Brusque.
As nove ficam marcadas com `alerta_automatico: false` e o motivo escrito.

```bash
python3 scripts/medir_mare.py     # troca esse julgamento por medição
```

O script lê a série já coletada e, por estação, calcula a amplitude diária, a
folga até a cota e quantas vezes o nível cruzou a cota em quantos dias. Quando a
amplitude é maior que a folga, a régua cruza sozinha. Ele **sugere** e mostra o
número ao lado; mudar quem dispara aviso continua sendo decisão de quem mantém
o projeto.

## Pendências

Em ordem de impacto.

### Quanto do mapa está apagado, e o que acende cada pedaço

Medido em 04/09/2026 por `scripts/conferir_cobertura.py` contra o `ultimo.json`
publicado. **Cinza não é defeito** — é o site se recusando a afirmar o que não
mediu. O que a tabela dá é o TAMANHO da recusa, para ordenar a quem pedir o quê:

| rio | animado | apagado |
|---|---|---|
| Itajaí-Açu | 184 de 323 km — **57%** | 139 km |
| Itajaí-Mirim | 50 de 186 km — **27%** | 136 km |

| km cinza | causa | como destrava |
|---|---|---|
| **122,3 km** | sem leitura **e** sem cota | Lontras, Ascurra, Botuverá, Guabiruba — as duas coisas |
| **84,2 km** | **sem cota** | **Vidal Ramos** tem nível ao vivo (2,51 m) e falta só o limiar — um número acende 45% do Mirim |
| **69,3 km** | **sem leitura** | Ilhota (36,4), Gaspar (16,9), Indaial (16,0) — têm cota oficial, e **três bloqueios diferentes** (abaixo) |

**"Sem leitura" não é um problema só — são três**, e confundi-los manda o
trabalho para o lugar errado (04/09/2026):

| cidade | a leitura existe? | o que trava de verdade |
|---|---|---|
| **Gaspar** 16,9 km | **SIM, pública** — estação 21 da DC, 1,83 m em 04/09 08:05 | **alcance**, não publicação: `defesacivil.gaspar.sc.gov.br` dá timeout da VPS (medido em 31/08, 01/09 e 04/09). `coleta_gaspar.py` já lê essa tabela e tem `--arquivo` para quem alcança de dentro da região. |
| **Indaial** 16,0 km | só no mapa da rede estadual | falta **fonte de leitura municipal** — a COMPDEC publica cota (6,00 m) e não publica nível |
| **Ilhota** 36,4 km | **não existe** | falta a **régua da própria cidade**. A DC-11 fica na divisa mas é de Itajaí, com zero próprio — usá-la seria misturar réguas |

O maior retorno por pedido continua sendo **a cota de Vidal Ramos** — um único
limiar acende 45% do Mirim —, mas o caminho mudou: **o `panel` da Asthon foi
capturado em 04/09 e NÃO tem a cota** (`band_thresholds`, `attention_level` e
`overflow_cota_m` vêm `null` para aquela régua, que publica só o nível, 2,50 m).
A hipótese está refutada, não pendente; ver `docs/API-ASTHON-COMPLETA.md`. O que
resta é a **EPAGRI** (ofício C5) ou a COMPDEC de Vidal Ramos. Depois vem
**Gaspar**, que é a mais barata das três acima: cota oficial no lugar, leitura
pública, e só um caminho de rede entre as duas.


- [x] **O último trecho do caminho até o morador passou a ser medido — 04/09/2026.** O site lê nível de rio por `raw.githubusercontent.com` (`web/src/dados/serie.ts`, `tempoReal.ts`, `nivelSc.ts`), e **ninguém vigiava dali para a frente**: o `saude_coleta.py` lê os arquivos LOCAIS da VPS e responde "a coleta rodou?" e "a fonte publicou?" — nenhuma das duas cobre `VPS → branch tempo-real → raw → navegador`. Se esse trecho travar, a coleta segue verde e o morador vê nível velho. A lacuna apareceu quando outra sessão leu um arquivo publicado "quase 7 horas atrasado" e concluiu que o `raw` serve cache de CDN. **Não reproduz:** três rodadas de medição deram 6, 13 e 15 minutos de idade — que é a idade esperada de um arquivo publicado a cada 15 min —, e a API do GitHub (que não usa o mesmo cache) devolveu o topo do branch no **mesmo segundo** do conteúdo que o `raw` entregou. Mais: a regra proposta ("nunca verificar deploy pelo `raw`") trataria como estorvo de verificação o que, se fosse verdade, seria **defeito de produção** — o site lê pelo mesmo `raw`. E um sintoma desses tem duas causas que uma leitura só não separa: cache do CDN, ou publicação da VPS travada. Em vez de adotar a regra, entrou a medição: **`scripts/conferir_publicado.py`** lê três relógios no mesmo instante (o que o `raw` entrega, o que a VPS gerou, e o topo do branch pela API) e responde `CAMINHO VIVO` / `PUBLICAÇÃO PARADA` / `CACHE DO RAW` / **`NÃO DÁ PARA DIZER`** quando a API não responde — porque escolher entre as duas causas mandaria consertar a metade errada do caminho. Travado por `scripts/teste_conferir_publicado.py` (21 testes, com cada limiar sabotado). **Rodar na VPS**, onde a API do GitHub é alcançável: `python3 scripts/conferir_publicado.py`. Triagem completa em `docs/TRIAGEM-CACHE-RAW-E-v8.9.md`.

- [x] **As cotas de rua de Gaspar e Brusque ganharam coordenada — 04/09/2026.** Um documento de viabilidade afirmava "Brusque e Gaspar 100% georreferenciadas" e recomendava colorir as ruas no mapa a partir disso. Conferido: o `cotas-ruas.json` consolidado **não tinha campo de coordenada nenhum** — elas existiam só nos brutos e ninguém as tinha trazido. Era pré-requisito silencioso de "colorir as ruas", e um mapa construído sobre o pressuposto teria pintado a rua errada. `scripts/juntar_coordenadas_cotas.py` fez a junção, com método medido antes de escolhido: **Gaspar por alinhamento de sequência** (a ordem do bruto foi preservada — as duas sequências casam em 3 blocos contíguos, 1.613 de 1.613, com só 2 pontos que a consolidação descartou — e é a ordem que resolve as 42 ruas repetidas), **Brusque por chave `(rua, cota)`** (a ordem se perdeu: o alinhamento casa 1 de 350; a chave casa 348). **1.961 linhas ganharam `lat`/`lon`**, e só elas mudaram — nenhum outro campo foi tocado. **Duas linhas de Brusque ficaram sem, de propósito:** "General Osório, 7,87" tem dois pontos reais a ~330 m e uma linha só no consolidado; escolher um seria pintar 330 m de rua errada. **O bruto `brusque-mymaps-cotas.json` (3.688 pontos, com coordenada) fica proibido** pelo motivo que o `_meta` dele mesmo dá — *"NÃO IMPORTADO: o campo `cota` não pôde ser identificado como nível de régua"* —, porque coordenada boa não redime cota não verificada; o script recusa por nome, com teste. **Blumenau e Rio do Sul seguem sem coordenada** e há teste travando isso: não há bruto georreferenciado para elas, e geocodificar por nome sem revisão manda o morador para a rua homônima do outro bairro. Travado por `scripts/teste_juntar_coordenadas_cotas.py` (19 testes, com a guarda de troca de linha sabotada — as duas nuvens são disjuntas por ~10 km, então coordenada de uma cidade dentro da nuvem da outra aborta).

- [x] **Gaspar: o par cota↔leitura foi PROVADO — 04/09/2026.** O teste que faltava foi executado de dentro da região: `/estacao/ver/21` e `/monitoramento` mostram **1,92 m no mesmo instante** (04/09 12:46), e o município publica **apenas duas estações de nível** — `/ver/21` **Rio Itajaí Açu Gaspar** e `/ver/73` **Ribeirão Belchior Central** (0,00 m). O que fecha o argumento não é só os números baterem: é a **ausência de uma segunda régua no Açu**. O modo de falha de Rio do Sul foi cota de uma régua com leitura de outra, as duas no mesmo rio; em Gaspar não existe essa segunda, e as cotas do Plano são do fluxograma "MONITORAMENTO RIO ITAJAÍ AÇU". Com uma régua só, cota e leitura não têm como se referir a pontos diferentes. **Consequência:** o `BLOQUEIO_NAO_PINTAR` de Gaspar cai, e com as 1.613 cotas de rua já georreferenciadas (acima) Gaspar passa a ser **a primeira cidade que pode ter as ruas coloridas por cota × nível**. Correção do que eu mesmo escrevi horas antes, quando dei Gaspar como bloqueada. **Ressalva que permanece:** o Plano não nomeia o ponto nem publica o zero da régua — o par está provado, o zero não está documentado. **Achado junto:** o Ribeirão Belchior Central é a 2ª estação de nível de Gaspar, confirmando o padrão do baixo vale (Gaspar, Ilhota e Itajaí monitoram ribeirões à parte, por represamento); as cotas dele **não foram lidas** — abrir `/estacao/ver/73`.

- [ ] **Brusque entra na fila do par cota↔leitura — descoberto em 04/09/2026, e corrige um "pode ir".** Com as coordenadas no lugar (acima), o passo seguinte seria colorir as ruas de Brusque por cota × nível. O documento de viabilidade dizia que Brusque "pode entrar já". **Não pode**, e falha pela condição que o próprio documento impõe: `cotas_verificado` de Brusque é **`false`** e `fonte_cotas` é **`null`** — as cotas de referência 4,80/6,00 nunca foram conferidas em fonte oficial. Pior, a **régua das cotas de rua está bem provada** (Ponte Estaiada: o `_meta` do bruto registra cota + lâmina = 8,96 m, o pico de 17/11/2023, em **183 dos 184** pontos com lâmina) enquanto a **régua da leitura ao vivo não está identificada** — as duas estações de Brusque em `estacoes_tempo_real` têm `regua: null`, e a leitura (1,45 m em 04/09 19:35) vem do `coleta_itajai.py`, isto é, da página da Defesa Civil **de Itajaí**. É a MESMA página em que a leitura de Rio do Sul apareceu como "Estação MKS" enquanto a cota era da "Ponte Dom Tito Buss". Ali o `conferir_par_regua.py` acabou provando **mesma régua** — o ponto não é que a página erre, é que **ninguém sabia**, e a resposta só veio de medir. Colorir 348 ruas sobre um pareamento não provado multiplica o risco rua a rua, e Brusque só parecia liberada porque o número dela não chama atenção. **A segunda fonte existe e já está mapeada:** a ficha `docs/cotas-municipais/brusque.md` registra o portal próprio da Defesa Civil de Brusque, `https://defesacivil.brusque.sc.gov.br/monitoramento`, com **6 réguas de nível minuto a minuto** e a câmera da ponte estaiada. É a página da régua a que as cotas pertencem — exatamente a outra ponta que o `conferir_par_regua.py` precisa. **Ação:** capturar essa página de dentro da região ou da VPS, cadastrar o par e rodar; se os dois números baterem, as 348 ruas de Brusque destravam. **Por que não foi feito agora:** o portal não responde deste ambiente (`connect_rejected`, igual ao de Gaspar — os dois usam a mesma stack DEXTAK), e escrever o leitor da página sem ver a página seria inventar a estrutura dela. O `conferir_par_regua.py` de hoje só sabe ler o painel da Asthon (`stations[].level_m`); Brusque vai precisar de um leitor próprio, escrito contra o HTML real, como o `coleta_itajai.py` foi. Mesmo caminho de Gaspar. **Nada foi colorido**, e as duas cidades georreferenciadas seguem bloqueadas pela mesma pergunta que trava Rio do Sul.

- [x] **A trava de maré das nove réguas de Itajaí foi MEDIDA — 05/09/2026.** Rodado na VPS sobre o ndjson mestre (17 estações, 6 dias). O cadastro trava DC-01 a DC-09 com **o mesmo texto** — *"oscilação de maré maior que a distância até a cota"* —, e a série diz que essa frase é literalmente verdadeira em **duas**: **DC-01** (oscila 0,99 m/dia contra 0,16 m de folga, 45% de maré, **26 travessias em 5 dias** sem enchente) e **DC-04** (0,82 contra 0,51, 51% de maré). As duas ficam travadas com razão. **Mas "a medição não confirma a trava" não quer dizer "destravar"** — as sete restantes são três situações diferentes, e destravá-las juntas repetiria o erro de travá-las juntas: (a) **DC-02 e DC-07** oscilam bem abaixo da folga, não são maré e as travessias são curtas — destravar é seguro; (b) **DC-03 e DC-06** *são* maré (45% e 51%), mas a oscilação é **menor que a folga** e a maré não alcança a cota — o motivo cadastrado é falso, a trava não; (c) ⛔ **DC-05, DC-08 e DC-09 têm a cota baixa demais**: ficam **46,8 h, 25,5 h e até 47,5 h acima da cota por travessia**. Destravá-las acenderia um aviso que não apaga, que é o defeito já conhecido de Rio do Sul — amarelo que nunca apaga é amarelo que ninguém mais vê. O que essas três pedem é **conferir a cota com a COMPDEC**, não destravar. A **DC-09** é o caso mais delicado: **9 cm de folga** e 42% de maré, falhando os dois critérios por muito pouco nas duas direções. **DC-11**, que hoje dispara, **não é maré** (40%) — isto corrige a medição anterior por correlação, e o teste de frequência é o melhor dos dois (foi construído para eliminar o falso positivo que a correlação dava, apontando "MARÉ" em Taió, a 200 km do mar) —, mas oscila 0,85 m contra 0,25 m de folga, com 7 travessias e uma de 45,5 h: é candidata ao mesmo problema de cota, não a uma trava de maré. **Nada foi alterado**; mudar quem dispara aviso é decisão de quem mantém o projeto. Detalhe e recomendação em `docs/MEDICAO-MARE-2026-09-05.md`. Independente da trava, o `motivo_sem_alerta` das nove precisa deixar de ser o mesmo texto para todas.
- [x] **Rio do Sul: eu li errado, e a correção veio de dentro do meu próprio número — 05/09/2026.** Eu havia escrito que a cota de 4,50 m está "abaixo do leito normal" e que a pergunta ia para a Defesa Civil de Rio do Sul. **Errado nas duas pontas.** O relatório do `medir_mare.py` diz **"1 travessia"**, e uma travessia é o rio cruzando de BAIXO para cima — se a cota estivesse abaixo do leito, seriam **zero**, porque o rio começaria a janela já acima. Escrevi "o rio nunca desce até a cota" ao lado do número que prova o contrário. **O que realmente aconteceu:** a janela de 6 dias caiu inteira dentro do evento de 01/09 (pico **6,78 m**, 22 cm da marca oficial de enchente) e do **esvaziamento controlado** que veio depois — em 05/09 o sistema estava com **12 de 12 comportas abertas**, reservatórios em 34% e 38%. O aviso ligado naqueles dias **não era falso**: havia cheia de verdade. Conferido contra o nosso próprio registro, que é o que dá confiança: `docs/TAIO-E-BARRAGEM-OESTE.md` anotou a Barragem Oeste em **17,2 m com 7 de 7 abertas** em 03/09 20:41, e a leitura de 05/09 dá **15,00 m com 7 de 7** — **−2,2 m em 36 h**, duas sessões independentes. **O erro do site não é a cota: é não mostrar o estado do sistema.** Um rio a 5,4 m com as comportas abertas significa o oposto de 5,4 m com elas fechadas — fechadas e subindo, a barragem está segurando e o pior pode vir; abertas e estável, o pico já passou. Detalhe em `docs/RIO-DO-SUL-COMPORTAS.md`.
- [x] **A lacuna do "estado das comportas" foi FECHADA — 05/09/2026.** `docs/AUDITORIA-JICA-2011.md` registrava que "não implica que o site da Asthon exponha estado de cada comporta — isso continua lacuna". O endpoint `public.asthon.com.br/public/dams?city_id=4214805` traz `comportas[].aberta` **por comporta**, mais `montante_m`, `jusante_m`, `percent_use` e `vertido`, para as **duas** barragens e sem autenticação. A API de Taió (Uniparking) já dava `comportasAbertas` como texto `"N de 7"` só para a Oeste; o `dams` cobre o sistema todo. O JICA aponta que a previsão de Rio do Sul não serve na prática porque a DEINFRA não informa a **vazão** de saída — ela continua não informando, mas o número de comportas abertas é o proxy operacional mais próximo, e está publicado.
- [ ] **Coletar o `dams` da Asthon — falta só ver o corpo do JSON.** Não foi escrito porque `public.asthon.com.br` não responde deste ambiente (`connect_rejected`), e escrever o leitor de um JSON que não se viu é inventar a estrutura dele — o mesmo motivo que segurou o leitor de Brusque. **Ação:** capturar de dentro da região ou da VPS (`curl 'https://public.asthon.com.br/public/dams?city_id=4214805'`), e o coletor sai contra o corpo real.
- [ ] **⚠️ A faixa de alerta de Rio do Sul parece ser MÓVEL — confirmar com a COMPDEC.** Duas notícias com 16 dias de diferença dizem que a Defesa Civil **ajusta a faixa por evento**: em 15/08, *"reduziu a cota de alerta… nova faixa entre 5,50 e 6,50 m"*; em 31/08, *"a faixa fica entre 6,5 e 7,5 m"*. **Confiança `media` — é imprensa**, não foi gravado em `estacoes.json` e não deve ser até confirmar. Se confirmada, a consequência é grande: o `band_thresholds` da API (4,50 / 5,50 / 6,50) seria **cadastro**, não a faixa operacional do dia, e o site **não deveria dizer "ALERTA" com base nele** para Rio do Sul.
- [ ] **Suprimir cor de alerta durante esvaziamento declarado — NÃO implementado, decisão de quem mantém.** A regra proposta é: comportas abertas **e** nível estável ou caindo → mostrar número e contexto, sem cor de alerta. ⚠️ **Suprimir alarme é a direção perigosa.** Antes de existir, precisa de teste provando que comportas abertas **com o rio subindo** continuam alarmando — senão a regra apaga o aviso justamente quando a barragem está vertendo numa cheia nova.
- [x] **O último trecho até o morador foi CONFERIDO no ar — 05/09/2026.** `conferir_publicado.py` rodou na VPS e deu **CAMINHO VIVO** nos três arquivos que o site lê: conteúdo com 13 min, topo do branch `tempo-real` com 13 min, e os dois batendo (`4b3709cf`). Confirma o que a medição daqui já indicava e fecha a dúvida sobre o cache do `raw`: não há atraso de CDN — o caminho `VPS → branch → raw → navegador` está saudável. A API do GitHub é alcançável da VPS (daqui dá 403), então o script dá o veredito completo lá.
- [x] **A maré medida do CIRAM entrou em produção — 05/09/2026.** `coleta_mare_ciram.py` rodou na VPS: **189 medidas** em Balneário Camboriú, última às 23:00 com **66,3 cm (0,663 m) e residual +18,3 cm**, idade 29 min. Florianópolis e Imbituba também respondem. E **Itajaí devolveu 384 linhas sem nenhuma maré observada** — confirmação independente, agora na nossa própria coleta, de que não há maré medida em Itajaí hoje. O carimbo em Brasília e a conversão cm→m funcionaram no dado real (23:00 local contra 02:29 UTC dá os 29 min corretos).

- [ ] **⚠️ Cotas de Rio do Sul vão mudar — obra de Melhorias Fluviais (em licitação, 2026).** O AIBH do Itajaí-Açu (2021) descreve o projeto estadual de rebaixamento do leito + comporta de controle no trecho **Rio do Sul–Lontras**; o usuário informou (02/09/2026) que a obra **não foi executada e está em licitação** neste ano. Quando sair, ela **muda a curva-chave** (mesma vazão → nível menor na régua), põe o nível de cheia sob **operação de comporta**, e **quebra a série histórica** de Rio do Sul — que é o ponto onde nasce o Açu e a primeira referência do tronco, logo afeta previsão a jusante. **Ação:** acompanhar a licitação (Defesa Civil de SC); ao concluir, revalidar as cotas (4,50/5,50/6,50/abrigos 7,00) com a Defesa Civil de Rio do Sul, pedir a **nova curva-chave**, e marcar em `enchentes.json` uma **quebra de série** ("antes/depois das melhorias") para os picos de Rio do Sul. Já anotado na `observacao` de Rio do Sul (`estacoes.json`). Detalhe em `docs/AIBH-ITAJAI-ACU.md` §3.
- [x] **Onde o Benedito entra no tronco — resolvido por fonte oficial.** O AIBH do Itajaí-Açu (CEDRO+ENGERA para o IMA, 2021) descreve o "trecho entre Ascurra e Indaial, a montante da confluência com o Rio Benedito, ~14 km": o Benedito entra **entre Ascurra e Indaial, a montante de Indaial** — confirma o diagrama da topologia e corrige a suposição antiga ("entre Indaial e Blumenau"). É fato geográfico descritivo (não a conclusão de cheia do estudo, que é de fonte interessada e não se usa). Gravado em `_topologia.afluentes_rios` e no `desagua_em` do Timbó. **Falta o Luís Alves** (antes/depois da régua de Ilhota): segue para o `achar_confluencias.py` na VPS — o `converter_tracado_rios.py` e a query do Overpass já foram estendidos para baixar `luiz-alves.geojson`.

- [x] **Distância AO LONGO DO RIO no `transito.json` (não mais só a reta).** `scripts/medir_distancia_rio.py` monta os segmentos soltos do traçado OSM num grafo e caminha pela água (Dijkstra), gravando `km_rio` como CONTEXTO — **sem mexer nos tempos**, que seguem do estudo JICA (dividir por velocidade suposta seria inventar precisão). Onde as duas pontas estão no traçado: Rio do Sul→Indaial 85,8 km, Gaspar→Ilhota 16,9 km, Ilhota→Itajaí 33,2 km — sinuosidade de **1,2 a 2,0×** a reta e velocidade implícita 3–9 km/h, coerente com o JICA. Fica de fora quem está longe do traçado (Blumenau, coordenada da estação ~3 km do talvegue) ou em braço não mapeado (cabeceiras). Travado por `scripts/teste_medir_distancia_rio.py`.
- [ ] **Ponto exato onde o Luís Alves entra** (antes/depois da régua de Ilhota). O Benedito já está resolvido (acima, pelo AIBH); o Luís Alves ainda não. Caminho pronto: na VPS, rebaixar o traçado do afluente (a query do Overpass em `docs/fontes-tempo-real.md` já inclui o Luís Alves), rodar `converter_tracado_rios.py` (gera `luiz-alves.geojson`) e `achar_confluencias.py --gravar` — que acha a confluência por geometria (ponto de menor toque com o tronco) e usa o grafo do tronco (Dijkstra) para dizer entre quais cidades cai, com guarda "não toca". Travado por `scripts/teste_achar_confluencias.py`.
- [x] **Cidades de OUTROS rios pintavam o Açu — corrigido em 04/09/2026.** `cidadesDoRio('itajai-acu')` devolve as 14 cidades do cadastro, e o mapa transformava TODAS em âncora, encaixando cada uma no ponto mais próximo do traçado. Seis não estão no tronco, e três estão em **outros rios**: medido — **Timbó 8,22 km** (Rio Benedito), **Rio dos Cedros 16,60 km**, **Ituporanga 28,03 km** (cabeceira Sul, que nem é desenhada), **Trombudo Central 10,56 km**, **Ibirama 2,62 km** (Rio Hercílio). Encaixadas à força, elas pintavam trechos do rio principal com a faixa da régua DELAS — o que o próprio `estacoes.json` proíbe em palavras ("a cheia delas não é a mesma que desce o rio principal"). **A direção perigosa é a calma:** Timbó verde enquanto o Açu sobe deixaria um trecho VERDE num rio subindo. Agora `RioParaCena.eixo` diz quem pode pintar — tronco mais cabeceiras paralelas, do cadastro — e uma guarda geométrica de 5 km barra a cabeceira cujo rio não foi desenhado (Ituporanga). **As de fora continuam como PINO:** o nível delas é informação boa. O limite é 5 km e não 1 porque a régua de **Blumenau** fica a 3,0 km do talvegue — a coordenada publicada é a da ESTAÇÃO —, e apertá-lo calaria a cidade com 97 registros desde 1852; há teste travando os dois lados disso. Ver `web/src/logica/eixoPinta.test.ts`.
- [x] **O Ribeirão Canhanduba não chegava no Itajaí-Mirim — RESOLVIDO em 04/09/2026.** `conferir_afluentes_chegam.py` mediu o vão: **578 m**, com o traçado morrendo na várzea. A busca por nome nunca o fecharia, e o motivo apareceu quando `baixar_vao_canhanduba.py` consultou o Overpass por **caixa** e encadeou por **conectividade**: o trecho final **não se chama Canhanduba — chama-se Rio Conceição** (3 vias, ~650 m). A cadeia é confiável porque são 650 m de canal para 578 m em linha reta — sinuosidade 1,12, normal em várzea; uma cadeia vagando seria muito mais longa. Entrou como **rio próprio** (`rio-conceicao`), não fundido: o OSM lhe dá outro nome, e juntar os dois faria o arquivo afirmar que 650 m de Conceição são Canhanduba. Agora `ribeirao-canhanduba chega em itajai-mirim (via rio-conceicao) a 0 m`, e as onze réguas seguem a menos de 0,5 km do curso delas. O conferidor passou a seguir cadeia, com a trava de que encostar num curso **também cortado** não vale — senão dois afluentes cortados se validariam um ao outro. Detalhe em `docs/tracado-ribeiroes.md`.
- [x] **Três cursos de Itajaí faltavam no mapa — RESOLVIDO em 04/09/2026.** Com as onze réguas virando pontos no Monitor, quatro caíam longe de qualquer rio desenhado: **DC-08 Rio do Meio 4,41 km**, **DC-03 SEMASA 2,32 km**, **DC-07 Portal I 2,25 km** e **DC-09 Bairro Murta 0,87 km**. Faltavam porque a consulta original do Overpass pediu só `waterway=river`, e ribeirão é `stream` e canal é `canal`. A consulta rodou na VPS, `converter_tracado_rios.py` gerou `ribeirao-murta`, `ribeirao-canhanduba` e `mirim-canal-retificado` em `data/rios/` (commit `72037eb`), e a medição fechou: **as onze réguas a menos de 50 m** do curso delas — DC-08 a 0,00 km, DC-03 a 0,03, DC-07 e DC-09 a 0,01 —, cada uma casando com o curso que o cadastro diz. **O tronco nunca esteve errado:** a DC-11, na margem do meandro da Volta de Cima, seguia a 0,09 km antes e depois, e a "linha reta" que aparecia num recorte era o meandro cortado pela moldura do gráfico. O teste deixou de guardar o retrato das quatro e passa a cobrar a regra — nenhuma régua sem curso desenhado —, então apagar um geojson volta a falhar dizendo qual régua flutuou. Detalhe em `docs/tracado-ribeiroes.md`.
- [ ] **Completar o traçado das duas cabeceiras pelo Overpass, para virarem mapa.** A confluência já saiu (ver Concluído), mas `data/brutos/rio-do-sul-rios-tracados.geojson` cobre só o trecho perto de Rio do Sul: o "Itajaí do Sul" tem **10,6 km** e o "Itajaí do Oeste" **9,9 km** — não são os cursos que vêm de Ituporanga e de Taió. Serviu para o ponto, **não serve como traçado de mapa**, e por isso continua em `data/brutos/`, não em `data/rios/`: desenhar esse pedaço como se fosse o rio afirmaria geografia errada na tela. Completar pelo Overpass como os outros. Ver `docs/API-ASTHON-COMPLETA.md`.

- [x] **A tela do Açu deixou de afirmar uma fila falsa — virou árvore.** O `/acu` mostrava `Taió → Rio do Sul → Ibirama → Apiúna → Indaial → …` como se a água descesse nessa sequência. Não desce: Taió (Itajaí do Oeste) e Ituporanga (Itajaí do Sul) são **cabeceiras paralelas** que se juntam em Rio do Sul; **Ibirama fica no Rio Hercílio** (afluente lateral, não elo); e **Apiúna** era estação de altitude (DCSC-00178 "(H)"), não régua de rio — saiu do eixo, **Ascurra** (DCSC-00003, confirmada no tronco por Overpass) entrou. Verificado em mapa + Overpass 02/09/2026. No `estacoes.json`, `ordem` do Açu virou `null` e a posição passou a `ramo` + `ordem_no_ramo`; `_topologia` guarda a `tronco_sequencia` (Rio do Sul → Ascurra → Indaial → Blumenau → Gaspar → Ilhota → Itajaí), as cabeceiras e os afluentes. A tela desenha três blocos (cabeceiras / tronco / afluentes), com seta de tempo só no tronco — e **o `/rios` do bot mostra os mesmos três blocos** (o Mirim segue em fila). **O validador (`scripts/validar_dados.py`) passou a ABORTAR** se a fila global voltar, se faltar `ramo`/`ordem_no_ramo`, se um `codigo_dcsc` sumir ou se uma régua sem aviso não disser o motivo — travado por `scripts/teste_validar_dados.py`. Fonte única: `docs/TOPOLOGIA-CANONICA.md`.

- [x] **A sequência das réguas DC aparece nas telas de rio.** As 11 coordenadas vieram dos marcadores do `Mapa.php` da Defesa Civil de Itajaí (`scripts/preencher_coordenadas_dc.py`), a `ordem_descida` foi gravada por `scripts/ordenar_estacoes_itajai.py` (projeção no traçado) e a lógica de agrupar por curso, ordenar montante → foz e tratar o par co-locado já existia em `logica/reguas.ts` — **mas só a tela de Itajaí a usava**. As telas de rio (`/acu`, `/mirim`) chamavam `ReguasDaCidade` sem `agrupadoPorCurso`, e as réguas saíam numa lista achatada, em que a de Limoeiro, 26 km rio acima, encostava na do estuário. **O que faltava era um guard:** `mostrarGrupos = grupos.length > 1` descartava, junto com o subtítulo redundante do curso, também a ORDEM e os braços — e na tela de rio há sempre um curso só (`reguasComCota` já filtra por rio), então nunca entrava. Separado em dois: o subtítulo só com mais de um curso; a ordem, os braços e o par co-locado, sempre. No Mirim, Itajaí agora sai **DC-10 (Limoeiro)** → *"daqui para baixo o rio se divide em dois braços paralelos"* → **braço do curso antigo** (DC-05, DC-06) e **braço do canal retificado** (DC-03, DC-04), com a ressalva de que DC-06 e DC-04 ficam no mesmo ponto e não há ordem entre elas. No Açu, DC-11 → DC-02 → DC-01. Os ribeirões (Murta, Canhanduba) já ficavam fora do eixo, porque `reguasComCota` filtra por rio.
- [x] **Abrigo mais próximo na tela de Itajaí (45 abrigos com coordenada).** `data/abrigos-itajai.json` traz os 45 abrigos oficiais (nome, endereço, capacidade, zona), reproduzível por `scripts/coleta_abrigos_itajai.py` (ArcGIS público da Prefeitura). A tela de Itajaí ganhou a seção **"Abrigo cadastrado mais próximo"** (`AbrigoMaisProximo`): a pessoa usa a localização do próprio aparelho e o site mostra os 3 mais próximos por distância real (`logica/abrigos.ts`, com teste). **Decisões de segurança gravadas no dado e na tela:** `situacao`/`lotacao` da fonte foram **deixados de fora de propósito** (cadastro, não estado atual — evitam virar "aberto agora"); a distância é dita **em linha reta, não a pé**; o abrigo sem nome não entra na sugestão; e a ressalva vem primeiro — **quem ativa abrigo e manda evacuar é a Defesa Civil (199)**. A localização fica no aparelho, não é enviada.
- [ ] **Conferir a cota do DC-10 (Bairro Limoeiro, Itajaí-Mirim) na Defesa Civil de Itajaí.** No cadastro (`data/estacoes.json`), a régua DC-10 tem cotas de referência **atenção 8,0 / alerta 9,0 / emergência 10,0 m** — muito acima das outras réguas do Mirim em Itajaí (1,5–3 m) e do próprio nível lido (≈4,6 m num dia calmo). Ou a régua do Limoeiro tem o zero bastante deslocado (é régua própria, plausível — Limoeiro fica bem a montante), ou os limiares estão errados no cadastro. Enquanto não confere na fonte oficial, **a faixa do DC-10 não é confiável** — o bot mostra o metro com a idade, mas a classificação de faixa dessa régua pode estar deslocada. Verificar na página da Defesa Civil de Itajaí (mesma fonte das outras DC) e, se for o caso, corrigir `cotas_m` de `DC-10` — com teste travando. Achado na análise das onze réguas DC (02/09).
- [ ] **EPAGRI/CIRAM: a fonte que pode fechar os `codigo_ana` que faltam.** O Boletim n° 150/2026 da Equipe de Hidrologia (em `data/brutos/`) publica **código ANA** por estação — é a primeira fonte do projeto que faz isso, e é o código que destrava a série histórica no HidroWeb, que é o que falta para Vidal Ramos, Taió e Ituporanga terem cota. Traz três estações da bacia que não temos: `83105000` Alfredo Wagner/Saltinho, `83892990` Vidal Ramos/Salseiro e `83029900` Taió/Barragem Taió Montante. **Nada foi atribuído:** a nossa régua de Vidal Ramos chama-se "Vidal Ramos" (Asthon, `owner_id: DCSC`, `-27.38547, -49.35812`) e a da EPAGRI chama-se "Salseiro", sem coordenada publicada — mesmo município não é mesma estação, e o teste que resolveria não fecha porque as leituras estão a 6 h uma da outra, com o rio subindo. Destrava pelo **"Rios On-Line"** da EPAGRI — mapeado pelo navegador em 01/09. O `robots.txt` libera, e a fonte é a mais promissora que o projeto achou para as cabeceiras: publica código ANA e **classifica cada estação em faixas** (os limiares que faltam a Taió, Ituporanga e Vidal Ramos existem do lado da EPAGRI). Mas o endpoint das estações (`POST .../estacoesMapa`, bacia do Açú = código 8) exige um header `Authorization` injetado em runtime — não o cookie. Dá para achá-lo lendo o bundle na VPS, mas é frágil: endpoint interno muda no próximo build. **O caminho principal virou o ofício C5** à EPAGRI (`sshidrosc@epagri.sc.gov.br`, rascunho pronto em `docs/pendencias-navegador-e-oficios.md`), pedindo o acesso documentado e a relação código ANA ↔ coordenada — que resolve o Salseiro e, se vierem os limiares, dá cota às três cidades. Detalhe técnico em `docs/fontes-tempo-real.md` (seção EPAGRI). **Três armadilhas registradas em `docs/fontes-tempo-real.md`:** os níveis estão em **centímetros** (254 = 2,54 m); o boletim é foto das 06:00 e diz "15 de 15 em normalidade" num dia em que às 23:35 Rio do Sul estava acima da cota de inundação — nunca pode aparecer como estado atual; e não traz cota de referência, então não dispara aviso.
- [x] **DC-11 é de Itajaí, não de Ilhota — resolvido pelo Plano de Contingência.** A estação `Rio Itajaí-Açu – Santa Regina (Volta de Cima)` estava cadastrada como `ilhota`, e a leitura dela disparava "Ilhota chegou à cota" no bot (aconteceu na cheia de 31/08, 3,38 m contra 3,00 m). A Tabela 11 do Plano da COMPDEC Itajaí (p. 23) a lista como estação **de Itajaí**, e a p. 12 põe Santa Regina e Volta de Cima na **ZONA 1 da Defesa Civil de Itajaí** — localidades do município, no extremo a montante, junto à divisa. Corrigido: DC-11 passa a `itajai` (é a única das onze réguas de Itajaí que fica acima da maré, por isso dispara aviso). **Consequência:** Ilhota fica sem nível ao vivo — como Gaspar antes do Plano —, porque nunca teve régua própria; a DC-11 na divisa é de Itajaí, com zero próprio. O aviso que saía com o nome de Ilhota estava errado e agora sai como Itajaí (Santa Regina). Testes no site e no `gaspar_estadual.py` travam a atribuição.
- [x] **Pino sem número dizia nada — corrigido em 04/09/2026.** Quando a cidade não tem leitura municipal nem bruto estadual, o pino mostrava **só o nome**, e duas situações muito diferentes ficavam idênticas na tela: **Gaspar** tem cota oficial (5/6/7 m), régua conhecida e estação cadastrada (`DCSC-00005`) — o que falta é a fonte publicar, e há ofício pendente —, enquanto **Guabiruba** não tem régua nenhuma no cadastro. Pino mudo em Gaspar faz quem mora lá concluir que o site não cobre a cidade dele, quando o que falta é um dado que alguém pode ir buscar. Agora o pino diz **"sem leitura"** (há régua, a fonte está calada) ou **"sem régua"** (não há instrumento). Nenhuma das duas se lê como "está tudo bem", que era o risco do nome sozinho. Conferido na tela renderizada. Ver `web/src/logica/pinoSemNumero.test.ts`.
- [ ] **A trava de maré é larga demais em pelo menos duas réguas — PRIMEIRA MEDIÇÃO em 04/09/2026.** As nove réguas de estuário de Itajaí estão com `alerta_automatico: false`, posto por julgamento sobre três leituras. Medindo a **série publicada** (`serie-recente.json`, janela de 48 h, 3 dias) as três dos ribeirões se separam com clareza:

  | régua | amplitude diária | folga até atenção | travessias | assinatura |
  |---|---|---|---|---|
  | **DC-07** Portal I | **0,01 m** | 0,67 m | 0 | **não é maré** |
  | **DC-08** Rio do Meio | **0,04 m** | 0,77 m | 0 | **não é maré** |
  | **DC-09** Bairro Murta | **0,40 m** | 0,34 m | 0 | **é maré** — oscila mais que a folga |

  Bate com a geografia: a DC-09 fica junto da confluência com o Açu; a DC-07 sobe o Ribeirão da Murta e a DC-08 fica bem ao sul. O motivo cadastrado nas nove é o mesmo texto ("variou mais de 50 cm em três horas"), mas **duas delas variam 1 e 4 centímetros por DIA** — herdaram a trava, não a mediram. Enquanto estiverem travadas, ribeirão urbano subindo **não dispara aviso e não pinta cor**, que é a direção perigosa: enxurrada ali é rápida.

  **Nada foi destravado**, e não deve ser por esta medição: (a) são só 3 dias, o mínimo do `medir_mare.py`, e a janela pode não conter maré de sizígia, quando a amplitude é maior; (b) a série publicada agrega por CIDADE, então as duas réguas da Murta foram separadas por um vão limpo de 0,22 m entre patamares — bom como diagnóstico, não como base para mudar quem alarma; (c) mudar quem dispara aviso é decisão de quem mantém o projeto.

  **Ação:** rodar `scripts/medir_mare.py` na VPS, onde a série `.ndjson` traz o campo `estacao` e separa régua a régua sem heurística, depois de mais dias. Se confirmar, destravar DC-07 e DC-08 — e decidir junto se o curso delas passa a **correr** no mapa, porque correnteza significa faixa (`web/src/logica/afluenteNaoCorre.test.ts` falha de propósito para forçar essa decisão).

- [ ] **PROVAR O PAR COTA↔LEITURA — a regra que três achados independentes exigiram no mesmo dia (04/09/2026).** Cota e leitura quase sempre vêm de páginas ou endpoints **diferentes**, e nome igual não prova régua igual. Três coisas convergiram nisto: (a) **Rio do Sul** — 88 de 88 leituras das últimas 48 h **acima** da cota de atenção, mínimo 0,83 m acima, porque a cota 4,50/5,50/6,50 é da **Ponte Dom Tito Buss** e a leitura vem da **Estação MKS**, outra régua, por outra página (a da Defesa Civil de *Itajaí*); (b) **as onze de Itajaí** — a página que o coletor lê (`/monitoramento/nivel-rios`) **não publica cota nenhuma**, conferido no `coleta_itajai.py`: as cotas vêm do Plano de Contingência, fonte diferente da leitura; (c) o `analisar_asthon.py` já registrava, desde antes, que as faixas de Dom Tito Buss aparecem **copiadas** em quatro outras réguas, com a regra "aplicar a cota de uma régua a outra cria alarme onde não há e cala onde há". **O método que fecha:** ler as duas fontes no MESMO instante e comparar o número — igual, é a mesma régua. **Nada foi alterado**: mudar quem dispara aviso é decisão de quem mantém o projeto, e as opções estão levantadas. O caso mais urgente é o Rio do Sul, onde a cabeceira do Açu fica **permanentemente amarela** no mapa — e amarelo que nunca apaga é amarelo que ninguém mais vê.

- [ ] **A DC-11 (Santa Regina) alarma e pinta, com 0,37 m de folga — e não foi medida.** É uma das duas réguas de Itajaí que **não** estão travadas (a outra é a DC-10), então dispara Telegram e recebe cor. Em 04/09/2026 marcava 2,63 m contra cota de atenção 3,00. Fica na Volta de Cima, ~10 km da foz — dentro do alcance da maré, na mesma faixa em que a DC-09 oscila 0,40 m/dia. **MEDIDA em 04/09/2026 — é de maré, e a afirmação do cadastro está errada.** O `medir_mare.py` rodou na VPS: 458 leituras em 6 dias, oscila **0,85 m/dia** contra **0,20 m** de folga, com **7 travessias da cota em 3 dos 6 dias**. Só isso não bastaria — a bacia estava descendo de uma cheia real na janela, e o script **não distingue maré de cheia** (mede amplitude e travessias, e chama as duas de "oscila"; num evento ele recomendaria travar régua que estava alarmando certo, que é o erro na direção que mata). A prova veio por outro caminho: tirando a média móvel de 13 h de todas as séries, some a recessão e sobra a oscilação rápida — e a da DC-11 correlaciona **+0,92 com a DC-09** e **+0,79 com a DC-03** (maré), contra **+0,09 com Blumenau** e **+0,17 com a DC-10** (rio acima). A parte rápida dela é maré: **0,33 m de pico a pico contra 0,20 m de folga**. Isso contradiz o `nota_cidade` do cadastro, que afirma ser ela "a única das onze que fica acima da maré (rio de verdade), por isso dispara aviso". Uma leitura ao vivo independente (04/09, 13h41) mediu **0,18 m** de folga, batendo com os 0,20. **A cota em si está bem documentada** (Plano v17, Tabela 11, `nome_no_plano` batendo) — o que está errado é a classificação, não o número. **Nada foi alterado.** Ver também a pendência do par cota↔leitura, acima. Contexto antigo, que continua valendo: Estava escrito aqui que "a série publicada junta as três réguas do Açu em Itajaí". Junta mesmo — mas essa é a `serie-recente.json`, o recorte que o SITE lê, e **não é o arquivo que o `medir_mare.py` lê**. Ele lê o **ndjson mestre**, que sempre guardou `estacao`, e chaveia por ela: as três saem separadas, e a DC-11 mede contra a cota DELA (3,00 m), não contra uma emprestada. Provado por `scripts/teste_medir_mare.py::AsTresReguasDoAcuEmItajaiSaoSEPARADAS`, que monta um ndjson com as três e cobra três medidas distintas — e falha se o script voltar a chavear por cidade. **O bloqueio real é banal: o ndjson mora na VPS**, e este ambiente não o tem. Um comando resolve, e o resultado fecha a pendência:

```bash
python3 scripts/medir_mare.py            # todas as réguas, com veredito
python3 scripts/medir_mare.py --json     # para colar aqui
```

Se a DC-11 for de maré, hoje é a única régua que pode tocar o alarme à toa: em 04/09/2026 marcava 2,63 m contra cota de atenção 3,00, e fica na Volta de Cima, ~10 km da foz — na mesma faixa em que a DC-09 oscila 0,40 m/dia. O `medir_mare.py` **sugere**, não decide: mudar quem dispara aviso é decisão de quem mantém o projeto.
- [ ] **Pedir à prefeitura de Itajaí as cotas por endereço.** O REST do ArcGIS foi sondado (`scripts/sonda_cotas_ruas.py` e `2`): a raiz abre sem token, com 108 serviços, mas a pasta `defesacivil` responde `499 Token Required` — é lá que o app "Cotas de Inundação" busca. O que é público em `historico_inundacoes` são as dez camadas de mancha, com 48/58/55/155 feições nas de 2013 a 2015: exatamente os arquivos do GeoItajaí que já estão no repositório. Ou seja, a cota por endereço de Itajaí **não é fonte aberta** — vira ofício, não código.
- [ ] **Perguntar à Prefeitura o que é a camada `Hidrografia_Terreno_Sujeito_Inundacao`.** Os 110 polígonos chegaram e somam **38,7 hectares**, com o menor tendo 4 m² — contra 7.086 ha da mancha de 1983. Não é "a área inundável de Itajaí", seja lá o que for. Fica fora da tela até haver dicionário de dados; virou o item 3 do ofício C2. Detalhe em `docs/tela-itajai.md` e `scripts/analisar_itajai_arcgis.py`.
- [ ] **"Meu ponto" na tela de Itajaí — bloqueado por referência, não por trabalho.** A especificação da tela pede comparar a elevação do terreno com o nível do rio para dizer "faltam Z m para a água chegar aqui". **Essa subtração não pode ser feita:** a elevação é altura acima do nível do mar (0,15 a 370 m) e o nível das estações DC é leitura na régua de cada uma, com zero próprio e não publicado. O resultado teria duas casas decimais e nenhum significado — e pareceria medido. Destrava com a cota por endereço do ArcGIS (ofício C2) ou com o zero de cada régua DC em relação ao mar. Detalhe em `docs/tela-itajai.md`.
- [ ] **Conferir se a estação DC-11 (Santa Regina, 3,00/4,00/5,00) existe no Plano de Contingência.** Uma especificação recebida a cita entre as estações de Itajaí; nosso cadastro tem DC-01 a DC-10 mais a DC-00. Se existir, é dado que falta — mas entra pela leitura do Plano, não pela transcrição da especificação.
- [ ] **Vidal Ramos: já tem nível ao vivo, falta a cota.** A régua da API Asthon **está na tela** desde 01/09 (`scripts/coleta_asthon.py`, ver Concluído): nível ao vivo com a idade à vista, faixa cinza. O que resta é a **cota de referência** (atenção/alerta) da régua — sem ela o número aparece e não dispara nada. Taió e Ituporanga, ao contrário, **não** têm régua de cidade na Asthon: têm as barragens Oeste e Sul, nível de reservatório na escala do próprio barramento, que nunca pintam faixa de cidade. Ver `scripts/analisar_asthon.py`. **O caminho Asthon FECHOU (04/09/2026).** O `panel` foi capturado na VPS, que alcança o host, e a régua de Vidal Ramos (`bd65df3e-a5e3-4760-a879-56df0fb90787`) vem com `band_thresholds`, `attention_level`, `overflow_cota_m` e `river_name` todos **`null`** — publica o nível (2,50 m às 11:35) e nenhuma cota. Não é captura incompleta: é ausência do campo na fonte, e a afirmação de que o endpoint "resolve o datum do Alto Vale para 21 réguas" está **refutada** para a única régua da bacia que importava. **Não repetir a consulta.** Restam a **EPAGRI** ("Rios On-Line" classifica por faixas — ofício C5) e a COMPDEC de Vidal Ramos. Continua sem gravar nada: o único número público é 3,50 m de transbordo (imprensa, 2015), e gravá-lo como cota faria Vidal Ramos e 84 km a jusante saírem **verdes** abaixo dele — cinza diz "não sei", que é a verdade. Ver `docs/API-ASTHON-COMPLETA.md`.
- [ ] **Cotas de rua de Itajaí.** As cotas por RÉGUA chegaram (Plano de Contingência); faltam as por rua, e a sondagem acima mostrou que elas não estão em fonte aberta. Itajaí segue sendo a cidade com manchas de inundação no repositório e nenhuma cota de rua. **Não usar `Relevo_Ponto_Cotado_Altimetrico` para preencher isso:** o campo `cota` dele é altura do terreno acima do nível do mar, não nível de régua. Mesmo nome, grandeza oposta.
- [x] **Mapa base do OpenStreetMap: carga sob pedido.** O chunk já era `lazy`, mas o componente era renderizado na primeira pintura — então **todo** visitante de `/itajai` baixava o Leaflet e puxava tiles, quisesse o mapa ou não. Medido: **161 kB de JS + 17 kB de CSS** (≈54 kB comprimidos), mais o GeoJSON do evento (até 175 kB no de 1984), mais os tiles. Agora o mapa entra por um botão. Isso custava duas vezes no mesmo momento: atrasava o número que a pessoa veio buscar, no celular e na rede pior, e multiplicava o tráfego nos tiles públicos numa noite de enchente — exatamente quando o site não pode cair. **Ainda em aberto:** medir o tráfego real depois de uma cheia e, se preciso, passar a um provedor de tiles ou servir os próprios; e reduzir o refetch ao trocar de evento, que hoje refaz `fitBounds` e busca outro conjunto.
- [ ] **Resolver a referência altimétrica de Blumenau** — teste no HidroWeb (estação 83800002, cotas de 09/07/1983 e 07/08/1984) ou resposta da FURB. Enquanto não sair, a regra bloqueante do `CLAUDE.md` vale: o site rotula cada ponto e recusa parear referências diferentes, ao custo de a previsão Rio do Sul → Blumenau ficar em "dados insuficientes".
- [ ] _(opcional)_ Seletor régua/IBGE nos gráficos de Blumenau, aplicando ±0,20 m só para visualizar. Só vale a pena se a verificação acima demorar — o gráfico já mostra a referência de cada ponto e avisa quando mistura.
- [ ] **Levantar os picos de Itajaí de 1983, 1984, 2001, 2008, 2011, jul e set/2013, jun/2014 e out/2015.** As manchas de inundação desses nove eventos já estão no repositório, mas nenhuma tem o nível do rio correspondente — a legenda do mapa fica sem dizer "isto foi com o rio em X m", que é o que tornaria a mancha comparável com o nível de hoje.
- [ ] **Conseguir as tabelas de cota de rua que faltam.** Rio do Sul e Blumenau saíram (ver Concluído). Brusque e Gaspar saíram também, pelos KML das respectivas Defesas Civis (ver Concluído). Resta **Itajaí** (ArcGIS fechado por token — ofício à prefeitura), que segue sendo a única cidade com manchas de inundação no repositório e nenhuma cota de rua. Hoje são **4.593 pontos**: 2.042 de Blumenau, 1.619 de Gaspar, 555 de Rio do Sul e 377 de Brusque.
- [x] **Coletar a API pública Asthon de Rio do Sul — feito para Vidal Ramos.** `public.asthon.com.br`, `city_id 4214805`. Das 29 estações do Alto Vale, `analisar_asthon.py` já dizia que **só Vidal Ramos** serve como régua de cidade; as demais são barragem (reservatório), altitude ou a cota da Ponte Dom Tito Buss (4,50/5,50/6,50) copiada para outra régua. `scripts/coleta_asthon.py` coleta Vidal Ramos por lista fechada de `station_id`, converte o carimbo de UTC para Brasília e é fiado no `coleta_niveis.py` (ver Concluído). Taió e Ituporanga **não** saem por aqui (só barragem) — seguem pela pendência do HidroWeb/ofício. As barragens ficam anotadas como sinal antecipado de cheia (subiram +12 m em 48 h em 01/09), nunca como nível de cidade.
- [ ] **Conferir as cotas de Blumenau contra o PDF oficial de 2014** (Farol Blumenau, bloqueia robôs — pelo navegador). As 1.938 estão como `confianca: media`; onde os dois baterem, ganham respaldo oficial, e onde divergirem vale o mecanismo de `divergencias`. O definitivo é a FURB, com entrega prevista para **novembro de 2026**.
- [ ] **Cota de referência de Vidal Ramos e Ituporanga — Taió saiu.** Taió ganhou as faixas do Plano de Contingência da COMPDEC em 04/09 (ver Concluído). Restam duas, e por motivos diferentes: **Ituporanga** não apareceu no levantamento de 41 municípios (não há arquivo dela); **Vidal Ramos** apareceu, mas a COMPDEC **não publicou tabela** — o que existe é comportamento observado (transborda "acima de 3 m" segundo a Defesa Civil citada pela imprensa em 2015, ~3,50 m num levantamento do mesmo ano, ~3,80 m de teto em eventos recentes). Não foi gravado: número de imprensa sobre comportamento não é faixa de acionamento, e a cabeceira do Mirim sobe em minutos — errar para cima ali é caro. Ver `docs/cotas-municipais/vidal-ramos.md`.
- [ ] **Rio do Sul mede os TRÊS rios — falta coletar e mostrar.** O portal e a API mostram régua no Açu (Ponte Dom Tito Buss), no Itajaí do Sul (Ponte Ricardo Kanitz, e ainda a Ponte Hannelore Hartmann Eyng) e, pelo portal, no Itajaí do Oeste (Ponte BR 470 — esta ainda **não confirmada** na API). Hoje o `coleta_asthon.py` traz só uma. É o **melhor ponto de calibração da bacia**: as duas entradas e a saída medidas na mesma cidade, com minutos de diferença, contra a proporção de área de drenagem conhecida (Sul 1.164 km² × Oeste 851 km², 58%/42%). Detalhe, ressalvas de comparabilidade e as perguntas para a Defesa Civil em `docs/reguas-rio-do-sul.md`.
- [ ] **AlertaBlu: destravar com dois comandos, não com argumento.** O coletor que chegou pronto continua fora, e agora há um argumento a favor dele — buscaria só assets (`/static/data/*.json`) e a página pública `/p/enchentes`, não as páginas do painel que o `robots.txt` restringe. O argumento é bom e **não é verificação**; quem o trouxe também marca a checagem como pendente. São **dois** bloqueios: o `robots.txt` nunca foi lido, e o servidor manda **cadeia de certificado incompleta** (`unable to get local issuer certificate`) — o segundo impede o primeiro. Os comandos estão em `docs/fontes-tempo-real.md`. **Nunca desligar a verificação de TLS**: abriria a coleta para qualquer um no caminho injetar nível de rio.
- [ ] **Trazer `blumenau-enchentes-registradas-alertablu.json` para o repositório — a análise já está pronta esperando.** É a tabela oficial de 102 enchentes de Blumenau (1852–2024), da página `/p/enchentes`. `scripts/conferir_blumenau_alertablu.py` roda no instante em que o arquivo aparecer e devolve um dos quatro vereditos: o AlertaBlu está em IBGE, está na régua, **muda com a época** (e aí não se converte nada), ou há uma terceira referência. Ele compara os pares rotulados IBGE com os sem rótulo em vez de tirar uma mediana só — é aí que o caso difícil se esconde, e é o que os indícios apontam: 1880, jul/1983 e ago/1984 batem ao centavo com o rótulo, e set/2011 fica 0,40 m fora.
- [x] **Cotas de régua de Gaspar — resolvidas pelo Plano de Contingência.** A cidade tinha 1.618 cotas de rua e nenhuma cota de régua; nada disparava aviso lá. O fluxograma do item 4.2.3 do Plano (p. 25) publica as quatro faixas: **0 a 5 m NORMALIDADE, 5 a 6 m ATENÇÃO/ALERTA, 6 a 7 m ALERTA/ALARME e ACIMA DE 7 m RESPOSTA**. Entraram como `atencao` 5,00 / `alerta` 6,00 / `emergencia` 7,00 — `emergencia` e não `inundacao` porque 7,00 m é a fase de resposta do Plano, e a primeira rua alaga 80 cm antes, a 6,20 m. **A margem é real:** a atenção fica 1,20 m abaixo da primeira rua, o oposto de Brusque. **Duas armadilhas no caminho:** o fluxograma é uma imagem que escreve "7 a 8 metros", e o PDF pinta por cima uma caixa opaca com "Acima de 7 metros" — vale o que o documento mostra, a faixa não tem teto; e o Plano **não nomeia o zero da régua**, então o que sustenta tratá-la como a mesma das cotas de rua é a coerência entre três publicações da mesma Defesa Civil, conferida por `scripts/conferir_gaspar_plano.py`: 24 das 26 vias do quadro do Plano batem ao centavo com o cadastro do KML, e a leitura de 31/08 (3,85 m) cai na normalidade do mesmo fluxograma. **O que sobrou é outra coisa:** ver o item da leitura de Gaspar, abaixo.
- [ ] **Nível de Gaspar: há cota, a leitura É PÚBLICA, e o que falta é ALCANÇAR o host.** Com as faixas cadastradas, o aviso automático passa a funcionar no instante em que houver um número. A tentativa óbvia é ler Gaspar pela rede estadual (`DCSC-00005`), contornando o host do município. **Três medições dizem que ainda não dá:** (a) no snapshot de 01/09/2026 03:09Z a estação **não devolveu nível** — trouxe chuva (77,2 mm/24h) e mais nada; (b) a rede não está num zero só — Ilhota vem **10,67 m** onde a nossa régua marca **3,34 m**, 7,33 m de diferença, e no dia anterior foram 7,09 m, enquanto Brusque bate (4,48 × 4,42); (c) as faixas de Gaspar são 5/6/7 m, e um deslocamento desse tamanho **cobre a escala inteira** — mostraria RESPOSTA com o rio no leito, ou normalidade com a água na rua. O que destrava é um **par medido**: nível estadual e leitura da tabela do município no mesmo instante. `scripts/gaspar_estadual.py` existe para juntá-lo e se recusa a propor o número para aviso enquanto `DESLOCAMENTO_CONHECIDO_M` for `None` — e também recusa par com mais de 30 min entre as duas leituras, porque numa cheia o rio sobe nesse tempo e a diferença sairia parte régua, parte subida. **Rodado na VPS em 01/09: a estação respondeu com carimbo fresco e sem valor de nível três vezes seguidas** (03:09, 03:24 e 03:33 UTC). Ela publica chuva, não régua — o mesmo que já valia para Blumenau (`DCSC-00026`, `rio_nivel: null`), a outra cidade da bacia com sistema municipal próprio. O caminho estadual está, na prática, fechado. **Correção de enquadramento (04/09/2026):** o que resta NÃO é pedir que Gaspar publique — a estação 21 da Defesa Civil publica, e foi lida em 04/09 às 08:05 marcando **1,83 m, NORMALIDADE**. O bloqueio é de **alcance**: `defesacivil.gaspar.sc.gov.br` dá timeout de conexão da VPS (medido em 31/08, 01/09 e 04/09; de dentro da região o navegador abre). Isso muda o pedido: o ofício C4 deixa de ser "publiquem o nível" e passa a ser **"por que o host recusa conexão de fora, e há endpoint ou espelho que aceite"** — junto com a pergunta da coluna "chuva atual" e a de Rua Lino × Rua Lírio. Enquanto isso, o caminho que já funciona é `coleta_gaspar.py --arquivo`: quem alcança salva o HTML e passa o arquivo, sem pedir nada ao servidor. **A legenda da própria página corrobora a cota** que veio do Plano: normal < 5,00 m, atenção > 5,00 m (ou chuva atual > 6 mm), emergência > 7,00 m — as duas pontas batem ao centavo com o fluxograma da p. 25, por publicações independentes da mesma Defesa Civil. **Divergem no meio:** a página **não tem faixa de alerta**, o Plano tem (5–6 atenção, 6–7 alerta). Ficamos com o Plano, que é o documento mais específico e o que dá a faixa intermediária — tirar o alerta só apagaria um degrau de aviso. Detalhe em `docs/fontes-tempo-real.md`.
- [ ] **Conferir "Rua Lino" × "Rua Lírio" com a Defesa Civil de Gaspar.** Nosso cadastro tem "Rua Lino" a 6,57 m, vinda do estudo do CEOPS pela imprensa; o KML oficial tem "Rua Lírio" com mínima de exatamente 6,57 m e nenhuma "Rua Lino". Provavelmente é a mesma rua com erro de transcrição, mas "provavelmente" não apaga registro — os dois ficam até alguém confirmar.
- [ ] **Pedir a planilha da Defesa Civil de Brusque — a 2ª etapa do levantamento.** O KML original chegou e a camada de 2023 foi importada (ver Concluído), mas ela cobre só os pontos atingidos até 8,96 m; a 2ª etapa, dos pontos não atingidos em 2023, continua sem fonte pública. A camada **"Cotas de Cheia 2011"** do mesmo arquivo segue **recusada**, agora com uma evidência a mais: cruzando as duas camadas por vizinho mais próximo, os oito pares a menos de 30 m diferem em +2,04 m na mediana, até +5,36 m — não é a mesma grandeza. Detalhe em `docs/cotas-de-ruas.md`.
- [ ] **Ler as células da Tabela 7.5.1 no PDF, p. A-80 — a existência já está confirmada.** A tabela **existe**: Volume III-A (Hydrology), p. A-80, "Largest Discharge Peak Time from each City, by Return Period", cobrindo as doze cidades. Os três números do Volume II são o **resumo** dela. A matriz por período de retorno está gravada em `transito.json._meta`, mas veio de **auditoria interna, não de leitura direta do PDF** — a extração truncou por volta da p. A-50. O que a sustenta é reprodução: ela gera, uma a uma, as seis células que já estavam gravadas por caminhos independentes. **Confirmar na p. A-80 antes de usar as células para MUDAR `horas_min`/`horas_max`.** Baixar `openjicareport.jica.go.jp/pdf/12043584_01.pdf`.
- [ ] **Pedir à DEINFRA a vazão de saída das barragens Oeste e Sul.** O próprio JICA aponta isso como a causa de a previsão de Rio do Sul não funcionar: *"DEINFRA, the operator of the Oeste dam and Sul dams… has not recorded and informed the outflow discharges"*. É confirmação institucional, não limitação de modelo. A API de Taió dá o **estado das comportas**, que é o proxy mais próximo — mas não a vazão. Novo ofício.
- [x] **Capacidades de vazão por cidade (JICA, Tabelas 3.6.3 e 3.6.4) — JÁ GRAVADAS** em `data/hidraulica.json`, bloco `capacidade_de_vazao`, com o `_ressalva_de_intervalos` que registra o próprio JICA dando números diferentes para a mesma cidade em figuras diferentes (Gaspar 4.000–6.000 numa, 5.100–6.000 noutra). A pendência ficou marcada como aberta por engano até 04/09/2026. Dizem quanto o canal aguenta antes de transbordar e a que período de retorno corresponde: **Rio do Sul 1.220 m³/s e Itajaí 2.000–3.000 — ambos ~5 anos**, os pontos mais frágeis da bacia; Indaial 5.700 (>50 anos), o mais robusto. Confirma por outra grandeza o que o site já mostra por cota. O Mirim na foz aguenta **300 m³/s (<5 anos)**.
- [ ] **Gravar a declividade por trecho (JICA, Tabela 3.6.2) e ponderar a janela por ela.** Itajaí→Blumenau é **1/20.000** (quase plano) e a confluência do Norte→Lontras é **1/85** (íngreme). A onda acelera no meio e arrasta no fim — é a razão física de a interpolação linear por distância ser ruim, e o argumento que faltava contra ela. Junto: *"the riverbed elevation in Blumenau city is lower than the mean sea level"*.
- [x] **Três pontos de curva-chave da cheia de 2008 (JICA) — JÁ GRAVADOS** em `data/hidraulica.json`, bloco `curva_chave_2008`, com guarda no validador (nível plausível, vazão positiva): Blumenau H=11,5 m → Q=4.200 m³/s · Timbó H=8,0 → 710 · Indaial H=6,0 → 3.100. São os primeiros pontos H→Q que o projeto tem.
- [ ] **Gravar a divisão 2/3 canal × 1/3 curso antigo do Mirim (JICA, seção 3.5).** Resposta direta para a topologia ramificada da foz. Junto vem uma controvérsia registrada em documento oficial: o canal retificado *"had caused an increase of flood discharge in the downstream reaches in Itajaí city"* — pode ter agravado a jusante.
- [ ] **Gatilho COMPOSTO de Gaspar — bloqueado por AMBIGUIDADE DA FONTE, não por trabalho.** A legenda de `/estacao/ver/21` diz que a atenção dispara por nível **ou chuva atual > 6 mm** — Gaspar pode entrar em atenção com o rio baixo, e nenhuma outra cidade da bacia tem isso. Investigado em 04/09 e **não implementado**, por dois motivos concretos: (1) **"chuva atual" não existe no nosso vocabulário** — a coleta principal guarda `min10/h1/h12/h24/h48`, e mapear "atual" para uma delas seria escolher por conta o que dispara aviso; (2) no próprio feed de Gaspar aquele campo **não é chuva atual** em parte das estações: três pluviômetros da "DC. GASPAR" publicam 42, 100 e 108 mm com a **última hora em zero**, o que é impossível. Ligar o gatilho hoje alarmaria todo dia, e alarme falso ensina a desligar o aviso — o dano que o `alerta_cotas.py` existe para evitar. **O que já foi feito:** a conferência de coerência passou a valer para Gaspar (ver Concluído), então as leituras ruins ficam marcadas e a régua oficial do rio, que é onde o gatilho se apoiaria, sai **coerente**. **O que destrava:** perguntar à Defesa Civil de Gaspar o que a coluna "chuva atual" acumula (10 min? o dia?) e qual das nossas janelas corresponde. Entra no mesmo ofício da "Rua Lino × Rua Lírio".
- [ ] **Trazer os 69 picos históricos de Gaspar (1852–2023) — falta o ARQUIVO.** `docs/GASPAR-DADOS-NOVOS.md` descreve `gaspar-historico-enchentes.json`, baixado do `/enchentes` da Defesa Civil, mas ele **não veio nos envios** e o site é inalcançável deste ambiente. Fecharia a lacuna "Gaspar sem nenhum registro". Junto vem uma regra que precisa entrar com os dados: a **retificação do canal na divisa Blumenau/Gaspar em 1986** (Santos & Pinheiro, Rev. Bras. de Geomorfologia, 2002) mudou a relação nível↔vazão — 55 eventos antes, 14 depois, e o maior pós-1986 é 9,80 m contra 12,56 m em 1880. **Nunca comparar pico pré e pós-1986**; é o mesmo tipo de quebra que a obra de Rio do Sul vai exigir.
- [ ] **Abrigos de Gaspar com cota própria — falta o ARQUIVO.** 28 abrigos, 14 com o nível do rio a partir do qual são atingidos (6 a 15 m). Nenhuma outra cidade publica isso. ⚠️ **Unidades incompatíveis**: Gaspar conta FAMÍLIAS, Itajaí VAGAS, Rio do Sul PESSOAS — nunca somar, e não converter por média. E a abertura é decisão da Defesa Civil evento a evento: a tela pode mostrar a lista com cota e situação, **nunca** mandar alguém para um abrigo.
- [ ] **Estado das COMPORTAS das barragens Oeste e Sul — lacuna que quebra previsão.** Coletamos o nível das barragens, não o estado das comportas. A Barragem Oeste tem **dois regimes**: retendo (amortece a chuva de montante) e **vertendo** (a água passa direto, e a Defesa Civil de Taió registrou "vertendo em mais de 1 metro"). Correlação calibrada num regime **subestima drasticamente** o outro — provável parte das correlações fracas no Alto Vale, junto com a topologia em árvore. Verificar se o `dams` da Asthon traz comportas. Ver `docs/TAIO-E-BARRAGEM-OESTE.md`.
- [ ] **Portal da Defesa Civil de Taió — não explorado.** É o 5º dos **só 5 portais municipais** da bacia (Itajaí, Blumenau, Rio do Sul, Gaspar, Taió); as outras 18 cidades dependem só da rede estadual, que dá nível bruto sem cota — **é por ausência de fonte municipal que `usar_para_cota=false` atinge tanta cidade, não por limitação do coletor**. Buscar cotas, histórico, abrigos e o estado das comportas. Pico conhecido: 12,11 m em 09/10/2023. Ver `docs/PORTAIS-POR-CIDADE.md`.
- [ ] **Exportar as cotas de rua de Rio do Sul pelo próprio site.** `index.php?r=soscota-rua/tabela` tem 555 itens e botão "Exportar Dados" — é a fonte exata dos nossos dados, e exportar é melhor que raspar. No mesmo menu, "Planilha Histórica Rio" fecharia o histórico da cidade.
- [ ] **Correlação oficial Blumenau↔Gaspar do CEOPS/FURB.** Existe estudo publicado que estendeu a série de Gaspar usando os picos de Blumenau "através de uma correlação", com períodos de retorno por Gumbel. **É exatamente a previsão a jusante que o projeto tenta construir**, já feita por quem tem a série completa e com método declarado. Pedir no ofício à FURB: o coeficiente e as cotas por período de retorno.
- [ ] **Gaspar tem ribeirões que REPRESAM — a janela de chegada não captura.** A Defesa Civil orienta quem tem cota entre 6 e 7 m a **esperar** antes de mover móveis, por efeito de remanso: o nível sobe por bloqueio de saída dos ribeirões somado à água do Alto Vale, não só pela onda do rio principal.
- [ ] **Responder às perguntas em paralelo — adiado de propósito (31/08/2026).** O `rodada()` responde em sequência, um POST bloqueante por vez, então a vazão é `1 ÷ latência do POST`: a 150 ms dá **~400 respostas por minuto**. O custo de CPU é irrelevante (1 ms em média; 9 ms no pior caso, `/rua`, que varre as 2.543 cotas), e o `getUpdates` traz 100 por chamada, o que não aperta. O total diário sairia em centenas de milhares — número que engana, porque ninguém pergunta do rio distribuído ao longo do dia: **todo mundo pergunta na mesma hora**. O que decide é o pico, e 400/min atende ~4.000 perguntas numa janela de 10 minutos de enchente. Acima disso forma fila; nada se perde, mas a pessoa espera. Como o teto do próprio Telegram é ~30 mensagens/s para chats diferentes, o envio em paralelo multiplicaria isso por 3 ou 4 sem esbarrar em limite externo. **Fica para quando o uso real justificar** — hoje não há medição de demanda que sustente a mudança.
- [ ] **Aviso automático para mais de um destino.** Hoje o aviso de cota vai para um único `TELEGRAM_CHAT_ID` do `.env`: o bot responde a quem pergunta, mas só alerta **uma** pessoa ou grupo. Um morador de Gaspar que nunca abriu o bot não recebe nada às três da manhã. Virar lista de inscritos traz consequências que o `bot.py` já registra: limite de envio do Telegram, gente recebendo aviso de cidade onde não mora, e responsabilidade sobre quem **não** recebeu.
- [x] **Chuva de mais onze cidades, da Defesa Civil de SC.** `coleta_chuva_sc.py` traz `chuva.acumulado` das estações da bacia — Blumenau, Ibirama, Apiúna, Botuverá, Guabiruba, Vidal Ramos, Taió, Ituporanga, Gaspar, Indaial e Ilhota. O `robots.txt` de lá libera tudo (`Disallow:` vazio). **Só chuva:** a mesma resposta traz `rio_nivel`, e ele não serve — Ilhota vinha 10,34 m enquanto a nossa régua marcava 3,25 m, as estações `(H)` trazem valores na casa das centenas, e não vem cota por estação, então nada disso poderia virar aviso. Milímetro, ao contrário de metro de régua, é a mesma grandeza em qualquer lugar.
- [ ] **Blumenau publica de três em três horas, e isso não é problema nosso.** Em 31/08/2026 a estação vinha com carimbo de ~3 h enquanto as outras treze da página de Itajaí estavam entre 18 e 29 min. A suspeita era de atraso de repasse. **Conferido no AlertaBlu, a fonte primária: ele mostra o MESMO valor com a MESMA idade** — 5,11 m há 3 h. A cadência é da estação, não do caminho; a nossa coleta está fiel. Consequências: (a) não há fonte pública mais fresca de Blumenau, e a tela mostrando "há 3 h" está certa; (b) coletar direto do AlertaBlu **não** melhora o frescor — continua valendo por ser fonte primária e ter série horária histórica, mas deixou de ser urgente; (c) o valor não é ignorável nem substituível, só velho, e a idade ao lado é a informação honesta. **Fechado (04/09):** a tela agora marca quando a leitura não é do agora **e** o rio subia quando ela foi tirada. O selo do nível passa a dizer, embaixo da idade, *"e subindo 20 cm/h quando mediu — o rio deve estar mais alto agora"*. Só nessa combinação: com leitura fresca o número já é o estado atual, e com o rio descendo ou parado o número velho erra para o lado seguro — subindo, ele erra para baixo, que é o lado que machuca. A `tendencia()` saiu de dentro do gráfico (`LinhaDoTempo`) para `dados/serie.ts`, com teste próprio, porque deixou de ser rótulo de linha e virou critério de leitura: o limiar de 2 cm/h separa movimento de ruído de sensor. Verificado no navegador nos dois casos (subindo → avisa; descendo, mesma idade → não avisa).
- [ ] **Nível das cidades que faltam.** A Defesa Civil de SC não resolve: sem cota por estação, nenhuma leitura vira aviso, e o "nível" que ela publica não é a mesma grandeza entre estações. Blumenau, em particular, vem com `rio_nivel: null` de lá — depende do AlertaBlu, hoje barrado por cadeia de certificado incompleta no servidor deles. Detalhe em `docs/fontes-tempo-real.md`.
- [x] **Série de chuva acumulada**, como já se fazia com o nível. Ela vivia só no `ultimo.json`, sobrescrito a cada rodada: quinze minutos depois o dado tinha sumido — e isso com os 29 pluviômetros de agora, não os 15 de antes. Vai para `data/tempo-real/chuva-AAAA-MM.ndjson`, separado do nível porque são grandezas com campos diferentes, e guarda as janelas e a marca de coerência: leitura incoerente entra **marcada**, não descartada, senão vira "não choveu" para quem ler a série meses depois. Falta ainda o que a torna útil: **parear a série com os picos**, que só dá depois de meses de coleta.
- [ ] **Aguardar a Defesa Civil publicar a maré.** O endpoint `ajax/mares.php` respondia `{"tides":[],"astronimical_tides":[]}` em 30/08/2026 — o gráfico do próprio site fica em branco nesse estado. O coletor já está escrito para o formato certo e passa a encher sozinho quando a fonte voltar. Enquanto isso, a tela da foz aceita a tábua digitada.
- [ ] **Levantar picos de Itajaí (foz).** Nenhum registro até agora — a tela da foz não estima altura nenhuma sem eles.
- [x] **Nível ao vivo nas cidades com mais de uma régua.** Itajaí tem onze réguas com zeros diferentes, e o site se recusava a eleger uma como "o nível de Itajaí" — o que é certo, esse número não existe —, mas o efeito era a **cidade da foz aparecer sem número nenhum** enquanto o dado estava ali. Agora saem todas, lado a lado, cada uma com o nome e com a cota **dela**. O aviso de que não se comparam vem antes dos números, não em rodapé. O pareamento é pelo **título exato** que a fonte publica, nunca por prefixo de código: conferido contra o arquivo publicado, 14 de 14 casam. Isso importa porque a DC-10, no Limoeiro, usa 8/9/10 m enquanto as do estuário usam 1,0 a 3,0 m — o mesmo 6,75 m é "abaixo de tudo" numa e alarme em outra. A previsão a jusante e a busca "minha rua" continuam desistindo com várias réguas, que é a REGRA BLOQUEANTE item 4. O `extrair_picos.py` **já estava resolvido** e eu afirmei o contrário sem conferir: ele tenta primeiro a cota da PRÓPRIA estação, e as dez réguas de rio de Itajaí têm a sua desde o Plano de Contingência — todas são analisadas. A única recusa é a DC-00, pluviômetro puro sem régua, e recusar ali é o certo.
- [ ] **Registrar o horário do pico** (campo `hora`, `HH:MM`) nos eventos. Só 2 dos 116 têm. É o que troca o hidrograma de projeto da JICA por medição de cheia real, em `calibrar_transito.py`.
- [ ] **Conferir o registro de 1911 de Rio do Sul no Portal GCD — alvo estreitado em 04/09.** A série local diz **maio**; o grande pico de Blumenau foi em **02/10** (16,90 m, a 2ª maior cota de lá). A cheia desce o Açu em **7 a 10 h**, então cinco meses não é imprecisão: ou são eventos distintos, ou uma das datas está errada. **O que a investigação mudou:** cruzando os **8 anos** com registro nas duas cidades, o mês bate em **7 deles** (1954, 1957, 1984, 2011, 2015, 2017, 2023) — **1911 é o único fora**. Não é uma série com datas ruins; é um registro isolado. E os 12,20 m poriam 1911 como o **4º maior** pico de Rio do Sul, magnitude compatível com o 2º maior de Blumenau — o que torna a hipótese de erro de mês plausível, **mas plausível não é conferido**, e mudar a data para ganhar um par de correlação é exatamente o que este projeto não faz. O validador agora avisa sozinho quando um par de cidades do tronco desalinha o mês (ver Concluído), então este caso não some de vista. Falta abrir o Portal GCD e ler o registro.
- [ ] Levantar picos de Gaspar, Ilhota, Indaial, Apiúna e Ibirama — hoje sem nenhum registro.
- [ ] Confirmar a posição de Guabiruba no eixo do Itajaí-Mirim (entrou pelo relatório-fonte, ainda sem carta oficial).
- [ ] Solicitar acesso à API da ANA (hidro@ana.gov.br) e conferir as rotas em `ana_hidroweb.py`, que ainda não foram validadas contra a API real.
- [ ] Verificar os códigos ANA já cadastrados (`verificado: false` em Taió, Rio do Sul e Blumenau).
- [ ] Localizar estações do Itajaí-Mirim e das cidades do Açu ainda sem `codigo_ana`.
- [ ] **Levantar cota de atenção e de alerta de Brusque — a maior lacuna do Itajaí-Mirim.** A cota de 4,80 m cadastrada como atenção é o nível em que a **Av. Beira-Rio, marginal ao rio, já começa a alagar**: a própria fonte a chama de "cota de inundação da via". Ela está como atenção porque é o primeiro sinal e porque a alternativa era avisar só a 6,00 m, depois de a água estar na rua. Mas **não existe faixa de aviso antes do primeiro alagamento**: em Brusque o aviso começa quando a água já está numa via, e num rio de resposta rápida como o Mirim é onde mais falta margem. Outros pontos alagam entre 5,46 m e 5,80 m, sem cota exata publicada — e a importação de 31/08/2026 achou pior: um ponto da **Av. Beira Rio esquina com Maria Scarpa Formonti, no Limoeiro, alaga a 3,76 m**, com 5,20 m de lâmina em 2023. São **1,04 m abaixo** da cota de atenção da cidade, ou seja, naquele ponto a água chega mais de um metro antes de o aviso tocar. O ofício C1 em `docs/pendencias-navegador-e-oficios.md` pede exatamente isso. Enquanto não vier, o site **e agora o bot** dizem o que 4,80 m significa, em vez de mostrar só o número.
- [ ] Levantar cotas de atenção/alerta/inundação das demais cidades; hoje só Rio do Sul, Blumenau e Brusque têm — e Brusque só a de inundação.
- [ ] Descobrir por que **Blumenau não aparece na coleta**. A cidade tem as três cotas cadastradas, mas a estação não vem no `ultimo.json` (o analisador já prevê o caso: "Blumenau às vezes vem vazio"). Enquanto não vier, a cidade com a série histórica mais longa do projeto fica sem aviso e sem nível ao vivo.

## Concluído

- [x] **O vigia passou a notar "cor sem alarme" — a conferência mapa × alarme roda sozinha, de hora em hora.** O script existia; faltava alguém rodá-lo. Agora `saude_coleta.py` o chama junto das outras checagens, e uma discordância entre os dois caminhos vira **falha com nome de cidade** no Telegram, em vez de esperar alguém lembrar de conferir. Foi assim que Blumenau ficou muda: o mapa pintava a faixa certa e o alarme recusava a cidade inteira, e **cor na tela parece aviso funcionando**. Mesma disciplina das outras checagens do vigia: **falhar em conferir nunca vira acusação** — sem o script, sem cadastro ou com erro de leitura, o veredito é ok com a ressalva no detalhe, porque alarme falso ensina a ignorar o alarme verdadeiro. Quatro testes.

- [x] **Conferência automática entre o MAPA e o ALARME — a falha do Blumenau não volta em silêncio.** O buraco existiu porque os dois são **caminhos separados**: a cor sai do `faixaDaCidade` no navegador, o Telegram sai do `alerta_cotas.py` na VPS. Cada lado, sozinho, estava coerente — e **nenhum teste unitário pegaria**, porque a falha só existe na discordância entre eles. `scripts/conferir_mapa_e_alarme.py` (novo) compara os dois contra o `ultimo.json` **publicado** e cobra uma regra: cidade com cota de acionamento no cadastro **e** leitura fresca que pode virar faixa **tem de ser vigiada** pelo alarme; recusa só passa se estiver numa lista **fechada** de motivos aceitos. **"A cidade tem mais de uma régua" ficou deliberadamente FORA dessa lista** — aceitá-lo às cegas reabriria exatamente o buraco do Blumenau, e há teste travando isso. **Falseamento conferido:** voltando o `alerta_cotas` a contar linhas, o script sai com código 1 e nomeia *"blumenau — cotas {atencao: 6.0, alerta: 6.5, inundacao: 7.4}"*; com o conserto, sai 0. Outro teste compara o vocabulário de cotas que pintam com o do `web/src/logica/tempoReal.ts`: se os dois divergirem, o script deixaria de cobrir justamente a cota que o mapa pinta e ele não conhece. **A lição que ele encerra em código:** cor na tela parece aviso funcionando, e não é.

- [x] **BLUMENAU ESTAVA SEM AVISO AUTOMÁTICO NENHUM — falha silenciosa, achada e consertada.** A cidade com **97 registros históricos desde 1852**, a mais documentada do projeto, não disparava Telegram em cota nenhuma. O motivo: desde que o **AlertaBlu** entrou como fonte de **resgate**, Blumenau aparece **duas vezes** no `ultimo.json` — a primária da Defesa Civil de Itajaí e o resgate, ligadas por `resgate_de`. O `alerta_cotas.resolver` contava **linhas**, via *"a cidade tem mais de uma régua e a cota cadastrada é por cidade"* e **recusava as duas**. **Nada denunciava:** o mapa continuava pintando a cor certa, porque a tela usa outro caminho; só o Telegram calava. Apareceu rodando `alerta_cotas.py --seco` contra o `ultimo.json` **publicado** — o tipo de conferência que a inspeção de código não substitui. **O conserto tem três partes, e as três importam:** (1) conta **réguas**, não linhas, usando o `regua_de` que o vigia já usava — a função mudou-se para o `comum.py` para os dois darem a MESMA resposta, porque foi a divergência entre eles que criou o buraco; (2) vigia **uma leitura por régua, a mais nova** — com a contagem certa e sem isso, Blumenau passaria a avisar **duas vezes** pela mesma travessia, que é o outro jeito de gastar o alarme; (3) o **estado** passa a ser guardado pela régua, não pelo título da linha — quando a primária esfria e o resgate assume, o título muda, e o estado órfão faria a travessia ser avisada de novo a cada troca de fonte. O `desfazer` (rollback quando o Telegram recusa) recebeu a chave certa junto do aviso: com a chave errada, uma travessia recusada ficaria registrada como avisada e **o aviso nunca mais sairia**. Sete testes, entre eles o caso das onze réguas de Itajaí, que **não** são resgate e seguem distintas. Falseamento conferido: voltando a contar por título, dois falham.

- [x] **As réguas de Itajaí viraram tocáveis: cada uma abre o painel dela.** Elas apareciam como pontos desde o #146 e não abriam nada — o toque continuava pegando o pino da cidade. Agora o teste de acerto da régua vem **antes** do da cidade, com raio **menor** (14 px contra 26): é isso que torna a DC-01 alcançável ao lado do pino de Itajaí, enquanto um toque largo continua pegando a cidade. O painel mostra o **nome do lugar**, o nível com a idade, **as cotas DAQUELA régua** — não as da cidade, porque em Itajaí os zeros são diferentes (a DC-01 usa 1,16/1,36/1,56 e a DC-10 usa 8/9/10) — e, quando não há cor, **o motivo por extenso**: omitir a razão faria a ausência parecer falta de dado, e aqui o dado existe; o que não afirmamos é a faixa. Um painel por vez, porque dois no mesmo canto se cobrem. A régua em foco ganha anel, como o pino de cidade selecionado.

- [x] **A faixa de MONITORAMENTO existe na tela — Taió deixou de aparecer verde no nível em que a COMPDEC já monitora.** Era a pendência aberta pelo conserto do alarme falso: recusar o vermelho falso tinha deixado Taió em "abaixo da atenção" (verde) entre 5,00 e 7,00 m, faixa que o Plano de Contingência do município chama de **MONITORAMENTO** — e cuja emergência é 9,00 m. Verde era certo sobre a cota e **mudo sobre a fase**. Agora há faixa própria, entre normal e atenção, com **cor, rótulo, velocidade de correnteza e posição na legenda** próprias. **Não é remapear degrau:** mapear `monitoramento` para `atencao` era a opção que o projeto recusou ao gravar os dados — nome de fase não se rebatiza, e chamar de atenção o que a COMPDEC chama de monitoramento afirmaria um aviso que ninguém deu. A cor (`#a3c93a`) é mais clara **além de** mais amarela que o verde, para a diferença sobreviver a quem não distingue verde de amarelo; a correnteza corre a 0,45 (entre 0,35 normal e 0,6 atenção), porque neste mapa a velocidade **significa** o nível e acelerar mais diria um perigo não declarado. Conferido contra a leitura real: Taió a **5,63 m** (04/09, 03:12) sai de `normal` e vira `monitoramento`, com 7,10 → atenção, 8,10 → alerta e 9,10 → emergência intactos. **O que NÃO mudou, de propósito:** o `alerta_cotas.py` continua começando a avisar na **atenção**. O mapa mostra a fase; o Telegram toca no que a COMPDEC declara como aviso — misturar os dois gastaria o alarme numa faixa em que a própria Defesa Civil só observa. `observacao_cota` (Ibirama) e `ativacao_plancon` (Timbó) continuam **sem** pintar: são observação de decreto e gatilho de gabinete, não fase de escala — promovê-las seria o mesmo remapeamento recusado.

- [x] **Gaspar saiu do cinza: a leitura municipal passou a chegar ao site.** Gaspar tinha **cota verificada** (5/6/7 m, do Plano de Contingência, com o PDF no repositório) e **coletor próprio** desde antes — e mesmo assim aparecia cinza e parada no mapa, porque a leitura ia só para `ultimo_gaspar.json`, que o site não lê. É o mesmo elo que faltava a Taió: o coletor existia, o caminho não. Agora `coleta_niveis.py` a traz para o `ultimo.json`, no molde do Asthon e do Taió (fonte à parte, exceção engolida — o cron encadeia com `&&`, e uma tabela municipal fora do ar não pode levar junto a publicação de todas as cidades), respeitando o `robots.txt` como o coletor próprio já fazia. **A escolha da linha é por IGUALDADE, não por semelhança**, e esse é o cuidado central: a mesma tabela publica `RIBEIRÃO BELCHIOR CENTRAL` com **1,68 m** — nível **plausível**, de outro curso — e `PLU. - ALTO GASPARINHO`, cujo nome contém "gaspar". Pego por engano, o Belchior faria Gaspar aparecer **"normal" com 1,68 m enquanto o Açu estivesse em 6 m**: silêncio no lugar de alarme, que é o pior desfecho deste projeto e o único que nada no sistema recusaria (1,68 passa na régua de plausibilidade). Oito testes travam a seleção, entre eles a leitura **real** de 31/08 (3,85 m, que cai em normalidade contra as cotas do Plano) e o falseamento: trocando a igualdade por `"gaspar" in rótulo`, três falham. **Ressalva registrada no código:** o Plano não nomeia o ponto nem publica o zero da régua, então a correspondência é entre duas publicações da mesma Defesa Civil sobre o mesmo rio — Gaspar tem uma régua só no Açu, mas se a Superintendência responder que são pontos diferentes, é o `leitura_da_cidade` que muda.

- [x] **As onze réguas da Defesa Civil de Itajaí viraram pontos no mapa do Monitor.** A foz aparecia como **um** pino azul de "várias réguas" — e os **ribeirões da Murta e do Canhanduba**, onde a enxurrada urbana acontece, não apareciam em lugar nenhum, embora as leituras chegassem frescas a cada quinze minutos. Agora cada régua com coordenada vira um ponto próprio, com o código e o nível ao lado. **A armadilha, e é grave: NOVE das onze têm `alerta_automatico: false`** — são réguas de **estuário**, e o cadastro registra por extenso que a oscilação da maré é maior que a distância até a cota (em 30/08/2026 essa faixa variou **mais de 50 cm em três horas sem enchente nenhuma**). Pintá-las pela cota deixaria o mapa **laranja duas vezes por dia, na maré** — e quem vê isso todo dia aprende a ignorar a cor, inclusive no dia em que ela for verdadeira. Então elas aparecem como **anel vazado**, com o número e a idade, e **sem cor de perigo**: mostrar o número sem a cor não é meia informação, é a informação inteira com a única parte que não sabemos afirmar removida. É a mesma regra que o `alerta_cotas.py` já usava para não tocar o Telegram com a maré — agora ela chega ao mapa. **DC-10 (Limoeiro) e DC-11 (Santa Regina) recebem cor**, pela cota **delas**, porque não têm a ressalva. O tom do anel é o azul-água da família do pino "várias réguas", **fora** da escala verde/amarelo/laranja/vermelho, que é o que separa "aqui tem medição" de "aqui tem perigo". Seis testes em `logica/reguasNoMapa.ts`, um deles contra os **dados reais** (trava que são 11 com coordenada e 9 de maré, e falha se o cadastro mudar em silêncio); o vocabulário de cotas é o mesmo fechado do `faixaDaCidade`, então `monitoramento` não vira vermelho aqui também.

- [x] **O Monitor da bacia ganhou fundo de mapa: Escuro, Satélite e Mapa.** A pendência estava aberta desde 03/09 como opção (b) do `docs/CAMADAS-DE-MAPA.md` — "exige desenhar tiles no canvas **ou** trocar o motor por Leaflet". Resolvida por um terceiro caminho, mais barato: **tiles no próprio canvas, sem Leaflet e sem mexer na projeção**. O que destravou foi medir em vez de supor: a dúvida era se tile Mercator alinharia com a projeção equirretangular do canvas, e alinha — na latitude central as duas têm a **mesma proporção** (o `cos(27°) = 0,89101` do enquadramento é o fator que o Mercator aplica ali), sobrando só a curvatura: **1,20 px de erro máximo num canvas de 900 px** (0,13%), medido na bacia inteira. Cada tile é desenhado na caixa que a própria `projetar` devolve para os cantos dele. **O teste que sustenta é de alinhamento**, não de forma: o pixel onde o mapa desenha Blumenau, Itajaí, Rio do Sul e Brusque tem de cair dentro do tile que geograficamente as contém — e falha se o Mercator virar conta linear (conferido: 14.848 px de desvio). **Escuro continua padrão por função, não por estética:** fundo com textura concorre com as faixas de alerta, e o satélite degrada justamente o dado mais delicado — o cinza "sem leitura" some contra a mata. Por isso sobre imagem entra **contorno escuro sob todos os traços, o cinza inclusive**, e a mancha do mar sai (a imagem já diz onde é água). **Atribuição é condição de licença:** fica visível e troca com a camada, com teste travando que ela vem de `FUNDOS[fundo]` e não de texto fixo; outro teste recusa qualquer URL do Google, cuja licença não permite embutir tiles. Tile que falha some sem derrubar nada (o chão escuro aparece), o cache vive entre renders para não repedir o mosaico a cada tique, e janela absurda devolve zero tiles em vez de travar o navegador numa noite de chuva. **Não foi possível ver com os olhos:** o ambiente de desenvolvimento bloqueia os três hosts de tile, então a verificação visual fica para o GitHub Pages.

- [x] **O vigia passou a ver o deploy que não desembarcou.** Achado em 04/09/2026: o fio de Taió funcionava na mão e nunca chegava ao site. A causa não era o código — a VPS tem **dois checkouts**, e `crontab -l` mostra o cron da coleta rodando de **`/opt/enchentes-vale-itajai`** enquanto o trabalho manual acontecia em `/root`. Um `git pull` no segundo não muda nada no primeiro, e o teste feito à mão passa, dando a impressão de que o conserto está no ar. **O vigia não pegava, e não por bug:** ele compara cada coleta com a ANTERIOR, então enxerga régua que **sumiu** e é cego para régua que **nunca chegou** — um deploy que não desembarcou não perde nada, logo não acusa nada. Já tinha acontecido antes: o comentário do `ULTIMO_NIVEL_SC` registra o vigia cego por 13 h "na migração pro /opt". Agora `saude_coleta.avaliar_versao()` mede a distância do checkout até `origin/main` e **falha quando está atrás**, dizendo quantos commits e em qual diretório. **Falhar em CONFERIR nunca vira "atrasado"**: sem rede, sem git ou fora de um checkout, o veredito é ok com a ressalva no detalhe — alarme falso de deploy ensina a ignorar o alarme verdadeiro de cheia. E a **manchete do aviso muda**: com a coleta viva e só o código atrasado, o Telegram diz "🚚 O código no ar está atrasado" com o `git pull` do diretório certo, em vez de "🛠 A coleta de nível parou", que mandaria procurar defeito onde não há. Sete testes, com um git de mentira. **Atenção ao ler o README:** a linha do cron do vigia aqui embaixo cita `/root`, e a da coleta na VPS usa `/opt` — conferir sempre com `crontab -l`, que é a fonte de verdade.

- [x] **Taió chegou à tela: nível da cidade e o estado das comportas da Barragem Oeste.** O coletor existia desde 03/09 e **não tinha caminho até o site** — não estava no cron, não era importado pelo `coleta_niveis.py` e não era levado pelo `publicar_tempo_real.sh`. Agora o `coleta_niveis.py` o chama no molde do Asthon (fonte à parte, falha engolida: uma API municipal fora do ar **não pode** derrubar a publicação de todas as cidades, porque o cron encadeia com `&&`), e o publicador leva o `ultimo_taio.json` junto. O **nível** entra no `ultimo.json` e vira faixa de verdade — a régua do Centro é a que as cotas do Plano da COMPDEC descrevem, então sai com `usar_para_cota: true`, ao contrário do bruto estadual. As **comportas** não cabem em `leituras` (não são nível de rio) e vão no arquivo próprio: é o único dado de **operação de barragem** da bacia, e o JICA aponta a ausência dele como a causa de a previsão de Rio do Sul não funcionar. **Dois defeitos achados no caminho:** (1) com `set -euo pipefail`, um `[ -n "$X" ] && …` com X vazio na ÚLTIMA linha do grupo que monta a árvore faz o publicador **morrer antes de publicar, calado** — `bash -n` não pega, porque a sintaxe está correta; era risco latente com o `ultimo_nivel_sc.json` (que existe sempre na VPS) e viraria pane real com o de Taió, que só passa a existir depois da primeira coleta boa; (2) o `--seco` anunciava só o `serie-recente.json`, então o ensaio calava sobre três dos quatro arquivos. O novo `scripts/teste_publicar_tempo_real.py` roda o publicador de verdade num repositório de mentira, com e sem os opcionais, e **falha** se o `:` sumir. O `alerta_cotas.py` foi conferido e **já estava certo**: o `faixa_de` dele percorre um vocabulário fechado, então `monitoramento` não dispara Telegram — o lado Python já fazia o que o TypeScript passou a fazer no conserto anterior. **Falta a UI das comportas:** o arquivo vai ao ar e ninguém o lê ainda.

- [x] **O mapa deixou de gritar EMERGÊNCIA na fase mais branda de cinco cidades.** Achado em 04/09/2026 ao conferir por que tanta cidade aparece cinza. `faixaDaCidade` tomava a cota mais alta **alcançada** entre TODAS as chaves de `cotas_m` e mandava **todo nome desconhecido para a cor mais forte**. Como cada cidade guarda, além das fases, marcas que **não são gatilho**, o resultado reproduzido era: **Taió 5,05 m** (`monitoramento`) → vermelho, **Ibirama 3,05 m** (`observacao_cota`) → vermelho, **Lontras 9,25 m** (`seguranca_observada`) → vermelho, **Timbó 5,05 m** (`ativacao_plancon`) → vermelho, **Trombudo Central 8,76 m** (`inundacao_historica`) → vermelho. Taió mede o tamanho do erro: o Plano da COMPDEC chama 5,00 m de **monitoramento** e a emergência dele é **9,00 m** — quatro metros acima; a leitura real de 03/09 (5,25 m), que a home da Defesa Civil de Taió pintava de **amarelo**, sairia **vermelha** no nosso mapa. **O `estacoes.json` já dizia o contrário, em quatro cidades** — *"que o código ainda não desenha"* (Taió), *"chave que o código NÃO desenha"* (Lontras e Timbó), *"não dispara aviso nenhum"* (Trombudo Central): a intenção estava escrita no dado e era **falsa no código**. O conserto é um **vocabulário fechado** de chaves que pintam (`atencao`, `alerta`, `inundacao`, `emergencia`), aplicado **só na cor**; cidade sem nenhuma delas fica **cinza** — não verde, que leria como seguro, e não vermelha, que inventaria aviso que a Defesa Civil não deu. **Não é rebaixamento de risco:** a escala verdadeira continua inteira (Taió 7,10 atenção · 8,10 alerta · 9,10 emergência, travado por teste) e o número segue na tela pelo `NivelAoVivo`, que escreve *"acima da cota de monitoramento"* com o nome que a fonte deu. **Por que só apareceu agora:** o defeito estava latente porque nenhuma dessas cidades tem leitura que possa virar faixa — todas cinzas. Quem o acordaria é o coletor de Taió, que grava `usar_para_cota: true` e volta a funcionar no mesmo branch. A lacuna que fica (Taió "normal" entre 5,00 e 7,00 m) está em **Pendências**.

### Sessão de 04/09/2026

- [x] **Áreas de drenagem do JICA Vol. III-A — e a divergência muda de dono.** Entraram em `data/hidraulica.json` (bloco `areas_drenagem`): bacia total 14.933,2 km², seis sub-bacias e dezenove estações. **O achado não é a tabela, é o conflito que ela resolve:** o repo registrava as áreas das barragens como "JICA × API estadual" (Oeste 1.042 × 851; Sul 1.273 × 1.164). O Vol. III-A traz **851,2 e 1.165,4** — ou seja, **concorda com a API estadual a menos de 0,2% e discorda do próprio JICA Vol. II**. A divergência é INTERNA AO JICA, e o Vol. II é o lado isolado. Isso importa porque a `chuva_equivalente_mm` é armazenamento ÷ área: a da Oeste sai **79,7 mm** pelo Vol. II e **97,5 mm** pelo Vol. III-A — 22% de diferença num número que o repo usa para explicar por que aquela barragem enche. As três delimitações ficam gravadas lado a lado, e o validador passou a **abortar** se a nota sumir ou se alguém "corrigir" uma área em silêncio. Traz junto um número que fala direto com a previsão a jusante: **Rio do Sul 5.041 km² → Indaial 11.276 km²**, a área mais que dobra (Hercílio 3.354 + Benedito 1.500) — menos da metade da água que passa em Indaial veio de Rio do Sul, que é a razão quantitativa de esse par correlacionar mal.

- [x] **O projeto cresceu para além do eixo: Lontras, Timbó, Rio dos Cedros e Trombudo Central.** Quatro cidades com cota levantada entraram — cada uma no lugar que a **evidência** sustenta, não todas do mesmo jeito. **Lontras é tronco**, entre Rio do Sul e Ascurra, por duas fontes independentes: o JICA 2011 (Tabela 3.6.2) lista os trechos "confluência do Norte → jusante de Lontras" e "Lontras → Rio do Sul", e o levantamento municipal a descreve como jusante imediato de Rio do Sul — a obra estadual leva o nome do trecho. Foi a primeira mudança na `tronco_sequencia` desde que ela existe, e obrigou a renumerar `ordem_no_ramo` de seis cidades. **Timbó (Rio Benedito) e Rio dos Cedros são afluentes laterais**: o Benedito desagua perto de Indaial (AIBH + JICA), e o Rio dos Cedros desagua no Benedito — o PLANCON de Timbó monitora "o Benedito OU o Cedros", o que confirma que os dois chegam lá. **Trombudo Central entrou SEM posição na árvore**, de propósito: a fonte diz em que rio a cidade está, mas não onde esse rio encontra o eixo, e aqui não se inventa o lado — a tela a mostra em "Outros pontos", com a nota de que ainda não tem posição, em vez de fingir uma. **As cotas seguiram a mesma régua de honestidade:** só Rio dos Cedros tem escala de três fases, e ela mapeia uma a uma (o "alarme" do plano virou nosso `emergencia`, preservando posição e força). O 9,20 m de Lontras é **número de imprensa** e ficou em `seguranca_observada`; o 5,00 m de Timbó é **gatilho de ativação do gabinete**, não faixa de aviso, e ficou em `ativacao_plancon`; o 8,71 m de Trombudo é **marca histórica** e ficou em `inundacao_historica`. Nenhuma dessas três chaves é desenhada pelo código, então nenhuma pinta cor que a COMPDEC não declarou. Todas com `cotas_verificado: false`. **O validador pegou um conflito real no caminho:** Timbó estava ao mesmo tempo na árvore e em `afluentes_monitorados`, o mecanismo antigo para "tem régua mas não é elo do eixo" — ele abortou com "escolha um lugar só". Timbó saiu da lista antiga, e a observação da JICA que vivia lá (o pico de Timbó ocorre JUNTO com o de Rio do Sul, porque a chuva cai na sub-bacia do Benedito) foi levada para a cidade, não perdida. Conferido no navegador: cada uma das quatro caiu na seção certa do diagrama, sem erro de página.

- [x] **27 testes meus não estavam rodando no CI — e nada ficava vermelho.** O CI executa cada arquivo **direto** (`python3 teste_x.py`), não por descoberta. Quando o bloco `if __name__ == "__main__"` fica no MEIO do arquivo, ele roda no import e as classes definidas ABAIXO dele nunca chegam a existir. Foi o que fiz duas vezes nesta sessão, anexando classes ao fim de arquivos que já tinham o bloco: `teste_validar_dados.py` rodava **9 de 30** e `teste_coleta_gaspar.py` **38 de 44**. Entre os órfãos estavam os seis da coerência da chuva de Gaspar e os vinte e um do validador — testes que eu apresentei como proteção e que não protegiam nada. Descoberto por acaso, comparando a contagem das duas formas de rodar. O `ci.yml` já dizia a regra em comentário: *"um teste que não roda na CI é um teste que não existe"*. Agora existe `teste_todos_os_testes_rodam.py`, que confere as duas coisas: nenhuma classe depois do bloco `if __name__`, e **a contagem da execução direta igual à da descoberta** em todo arquivo — este segundo pega qualquer jeito de esconder teste, não só o do `__main__`. O total que o CI roda foi de **855 para 882**.
- [x] **Os dados hidráulicos do JICA em `data/hidraulica.json`.** Declividade por trecho (o trecho mais íngreme, **1/85**, fica no MEIO da bacia; o quase plano, **1/20.000**, é o de baixo — é o argumento físico contra interpolar a janela por distância), capacidade de vazão por cidade (**Rio do Sul 1.220 m³/s e Itajaí 2.000–3.000, ambos ~5 anos**, os pontos frágeis; Indaial 5.700, >50 anos, o mais folgado), as três barragens (a Oeste enche com **80 mm** de chuva sobre a bacia dela, metade do que a Norte precisa — por isso verteu em 2001 e 2010), os três pontos de curva-chave de 2008 e a divisão **2/3 canal × 1/3 curso antigo** do Mirim. **Três ressalvas da auditoria foram gravadas junto, não omitidas:** as áreas de drenagem das barragens **divergem** entre JICA e API estadual (delimitações diferentes — as duas ficam lado a lado, e há teste exigindo isso); o próprio JICA traz intervalos diferentes para Gaspar em figuras diferentes, que não se fundem; e o retorno de **8.400 anos** para 2008 **não foi confirmado** na fonte, então não foi gravado — o validador recusa se ele voltar como VALOR, mas permite citá-lo na prosa com a ressalva (o primeiro guarda que escrevi proibia a string e acusava o próprio texto que avisa para não gravar). **Achado de projeto:** a divisão do Mirim NÃO entrou em `_topologia`, porque é a presença desse campo que faz o validador tratar o rio como ramificado — o Mirim é fila, e teria quebrado inteiro. Há teste travando essa decisão.

- [x] **`coleta_taio.py`: o nível de Taió e o ESTADO DAS COMPORTAS da Barragem Oeste — a única fonte de comportas da bacia.** O relatório da JICA (2011, seção 4.2.2) aponta essa ausência como **a causa** de a previsão de Rio do Sul não funcionar: *"DEINFRA, the operator of the Oeste dam and Sul dams… has not recorded and informed the outflow discharges from the dams"*. A API municipal de Taió (Uniparking, **sem autenticação**) não dá vazão — ninguém dá — mas dá `comportasAbertas: "N de 7"`, que é o proxy operacional mais próximo e o que separa os dois regimes da Oeste: **retendo** (amortece a chuva de montante) e **vertendo** (a água passa direto). Correlação calibrada num regime subestima o outro, e é provável parte das correlações fracas no Alto Vale. Junto vem o nível do Centro, que fecha a lacuna de a cabeceira do Oeste só ter barragem. **A armadilha que o coletor existe para evitar:** o mesmo JSON traz `nivelCentro` (~5 m, a cidade) e `montante` (~17 m, o reservatório) — e a régua de plausibilidade do projeto **não pega** essa troca, porque 17,2 m cabe na faixa 0–25 m que ela aceita, enquanto a emergência de Taió é 9,00 m. Um coletor que lesse o campo errado pintaria emergência todo dia. A separação é estrutural, com teste travando. Outro cuidado: **não se converte fuso** aqui, ao contrário do coletor da Asthon — `dataUltimaAtualizacao` já vem em horário de Brasília, que é o contrato do `medido_em`; converter "para garantir" jogaria a idade 3 h fora. Dezoito testes contra a leitura real de 03/09 salva em `data/brutos/`, incluindo que `"0 de 7"` é dado BOM (barragem retendo) e não ausência, que `"8 de 7"` é recusado, e uma conferência cruzada: os 5,25 m caem na faixa de monitoramento do Plano — que é a cor que a home da Defesa Civil mostrava naquele instante. **Rodado contra o host na VPS (04/09) — e achou um bug do projeto inteiro, não de Taió:** a API respondia HTTP 400 com corpo vazio e sem `Content-Type`, assinatura de recusa na borda, antes da aplicação. O `curl` da mesma VPS respondia 200, o que fazia a linha de comando "provar" que estava tudo bem enquanto o coletor falhava. A causa era o nosso `USER_AGENT`: cabeçalho HTTP é ASCII, o `requests` codifica em **latin-1**, e o "ó" de "repositório" saía como o byte solto `0xF3` — inválido em UTF-8. O `curl`, num shell UTF-8, mandava dois bytes e passava. O teste que separou as três formas do mesmo caractere fechou o caso: UTF-8 dois bytes → 200, latin-1 um byte → **400**, só ASCII → 200. O `USER_AGENT` perdeu o acento e `scripts/teste_comum.py` trava o ASCII, na string e no byte que sai no socket. Valia para os **onze** scripts que importam de `comum.py`: as outras fontes toleravam o byte inválido, a de Taió não.

- [x] **A Tabela 7.5.1 da JICA EXISTE — e a "sobreposição" era mistura de períodos de retorno.** A questão que eu tinha deixado em aberto ontem foi resolvida na fonte: a tabela está no **Volume III-A (Hydrology), p. A-80**, com o título *"Largest Discharge Peak Time from each City, by Return Period"*, cobrindo as doze cidades; os três números do Volume II são o **resumo** dela. Ou seja, a recusa da reestruturação por interpolação estava certa e pelo motivo certo — as intermediárias têm tempo de fonte. **Mas o meu diagnóstico também estava errado, e do mesmo jeito:** eu comparei o 10 h de Indaial com o 7 h de Blumenau e conclui "sobreposição, não contradição". Os dois são de **colunas diferentes** — 10 h é o cenário de 5 anos, 7 h é o de 25/50. A regra correta não é "sobreposição é tolerável", é **nunca comparar célula de um período de retorno com outra**. Conferido célula a célula de onde saiu cada trecho gravado: os dois que "atravessam colunas" são exatamente as duas faixas afirmadas (7–10 e 14–17). **Segunda correção, esta no validador que escrevi ontem:** ele ABORTAVA quando o montante passava do máximo do jusante, sob a premissa de que é fisicamente impossível. Não é — nas colunas de 25 e 50 anos **Blumenau pica antes de Indaial** (+7 h contra +8 h), porque o Rio Benedito entra em Indaial e adianta o pico de baixo. Manter como erro **rejeitaria dado oficial verdadeiro**; virou aviso, e o aviso passou a nomear as três causas possíveis (mistura de coluna, afluente no meio, dado errado) em vez de sugerir defeito. A matriz ficou no `_meta` com a evidência declarada: as células vêm de auditoria interna, **não** de leitura direta do PDF — o que as sustenta é reproduzirem, uma a uma, seis valores já gravados por caminhos independentes.
- [x] **Gaspar: as duas fontes oficiais discordam, e a divergência ficou registrada em vez de escolhida em silêncio.** O Plano de Contingência (PDF no repositório, p. 25) põe a atenção em **5,00 m**; a legenda da estação no site da Defesa Civil põe a normalidade **abaixo de 6,00 m**. São do mesmo município e se contradizem na fronteira 5–6 m — meu enquadramento de ontem ("duas apresentações, não duas verdades") foi generoso demais. Adotado o **5,00 m**, que avisa mais cedo, com o 6,00 m gravado em `cotas_divergencias` com fonte, data e o porquê. O custo de escolher o outro lado é medido, não retórico: a primeira rua alaga a **6,20 m**, então a atenção a 6,00 deixaria **20 cm** de margem contra 1,20 m — e há teste travando a margem em pelo menos 1 m. Registrado também o **gatilho composto** da legenda (atenção por nível OU chuva acima de 6 mm), que só acrescenta aviso, com a explicação de por que ainda não dá para implementá-lo.

- [x] **Quatro cidades saíram de "sem cota nenhuma" para faixas oficiais: Taió, Ibirama, Ilhota e Indaial.** Chegou um levantamento de 41 municípios; ele foi tratado **fonte a fonte**, não em bloco. Entraram as quatro com documento nomeável: **Ibirama** é a mais forte — PLAMCON homologado por **decreto** (nº 5.431/2024, publicado no DOM/SC): observação 3,00 / atenção 3,50 / emergência 4,00 m. **Taió**: Plano de Contingência da COMPDEC, jan/2026 — monitoramento 5,00 / atenção 7,00 / alerta 8,00 / emergência 9,00 m. **Ilhota**: PLANCON 2025/2028 v016 — atenção 9,20 / alerta 10,00 / emergência 10,50 m. **Indaial**: 6,00 m, o único número que a COMPDEC publica. **As quatro com `cotas_verificado: false`**, porque nenhum dos PDFs está em `data/brutos/` e os domínios são inalcançáveis daqui — diferente de Gaspar, cujo PDF está no repositório e tem script que reconfere. **A regra que guiou o mapeamento:** nome de fase não se rebatiza. Taió tem CINCO fases para os nossos quatro nomes, e empurrar tudo um degrau faria a tela chamar de "atenção" o que a COMPDEC chama de monitoramento — afirmar um aviso que ninguém deu. O piso de 5,00 m ficou em `monitoramento`, e o de Ibirama em `observacao_cota`. Ilhota é o caso oposto: quatro fases para quatro nomes, uma a uma, então "prontidão" virou `alerta` — muda o nome, preserva a **posição e a força**, que é o que protege quem lê. **Indaial recebeu a ressalva mais dura:** os 6,00 m não são faixa de aviso, são o nível em que doze vias JÁ alagam; está gravado como `atencao` porque é o único número que existe, mas a observação diz com todas as letras que não é aviso prévio, é o começo — mesma situação de Brusque. **Ascurra foi RECUSADA:** a fonte é um post de Instagram da Prefeitura, e a escala (8,5–10,8 m) destoa de Indaial (6 m) e Rio do Sul (4,5–6,5) a ponto de o próprio levantamento avisar para não pintar sem confirmar a régua. Defeito achado e corrigido no caminho: o bot listava chave nova **no fim**, então "Monitoramento 5,00 m" saía depois de "Emergência 9,00 m" — uma escada que sobe e desce. As duas fases novas entraram em `ORDEM_COTAS` e ganharam rótulo. Cinco testes travam as cotas de Taió, inclusive contra o remapeamento de um degrau e contra emergência acima do maior pico conhecido (12,40 m em 2023). Os 41 levantamentos ficaram em `docs/cotas-municipais/`.

- [x] **O validador passou a conferir o MÊS entre cidades do tronco — e achou 1911.** A cheia desce o Açu em **7 a 10 h** (Rio do Sul → Blumenau), então duas cidades do tronco que registram o mesmo evento registram no mesmo mês com folga de sobra. Mês diferente não é imprecisão: são eventos distintos, ou uma data está errada. Nos dados reais, **1911 é o único desalinhado dos 8 anos compartilhados** — Rio do Sul em maio, Blumenau em 02/10, cinco meses de diferença; os outros 7 batem. Isso reposiciona a pendência: não é uma série com datas ruins, é **um registro isolado**. Três decisões que evitam alarme falso: a comparação é por **mês**, não por dia (a série de Rio do Sul é quase toda de precisão mensal — exigir o dia recusaria dado bom); o teste é "existe **algum** evento de jusante no mesmo mês", não "todos batem" (Blumenau tem 113 registros, vários por ano, e uma cheia de montante casa com UMA delas); e só entram cidades **do tronco** — cabeceira e afluente entram de lado, e comparar mês com elas afirmaria uma fila que a topologia nega. Vira **aviso, nunca erro**: a data pode estar certa e ser evento distinto, e quem decide é a fonte. Sete testes, incluindo que ano sem registro a jusante não conclui nada (ausência de dado não é desalinhamento) e que data só com ano é ignorada.

- [x] **O gráfico de picos passou a dizer que a régua não mede a encosta — o aviso que faltava sobre 2008.** O gráfico ordena enchentes por metro de rio, e o Vale tem um contraexemplo enorme: **novembro de 2008 é o 32º maior pico de Blumenau** nos nossos 113 registros, com **31 enchentes mais altas acima** (1880 marcou 17,10 m; 2008, 11,52 m) — e foi o evento mais letal da história da região. O motivo já estava no nosso próprio dado, na palavra da Defesa Civil de Blumenau: *"as mortes vieram sobretudo dos deslizamentos"*. Só que essa nota vivia dentro do `<details>` fechado, na coluna Fonte, entre 113 linhas — quem olhava o gráfico via a barra de 2008 mais baixa que a de 1983 e concluía "evento médio". **Era a conclusão exata que este projeto existe para não deixar acontecer.** Agora o aviso fica logo abaixo do gráfico, em vermelho (as outras ressalvas são âmbar: elas dizem que um número pode estar deslocado, esta diz que a lista inteira pode levar a pessoa a subestimar o que mais matou). O exemplo concreto — data, cota e posição — é **calculado dos dados em tela**, não escrito à mão, então acompanha a série. Duas decisões de honestidade: o aviso é **do gráfico, não de cada ponto**, porque marcar só os eventos cuja nota fala de deslizamento faria a AUSÊNCIA da marca dizer "aqui não houve" — e não sabemos isso de evento nenhum, já que não temos série de deslizamento; e um evento de encosta que também seja o pico mais alto **não** vira exemplo, porque aí não há inversão a denunciar. Seis testes na lógica pura, um deles travando que a nota de 2008 continua no `enchentes.json` — o aviso lê a fonte, não inventa.

- [x] **A chuva de Gaspar passou a ser conferida — e três estações não fecham.** O coletor de Gaspar lia as seis janelas de chuva e **nunca conferia se elas fechavam**; a checagem existia só no coletor de Itajaí. Agora vale para os dois, pela **mesma função** — `incoerencias()` ganhou o parâmetro `ordem` em vez de uma segunda cópia, porque regra duplicada é regra que diverge, e é sempre a cópia esquecida que passa a aceitar lixo. O que apareceu não é hipotético: na página real salva no repositório, **três pluviômetros da "DC. GASPAR" publicam `chuva atual` de 42, 100 e 108 mm com a última hora em ZERO**. Se aquilo tivesse caído agora, a última hora não poderia marcar zero — o campo não é chuva atual naquelas linhas, seja lá o que for. Isso importa porque a **atenção de Gaspar dispara com chuva acima de 6 mm**: um gatilho apoiado nesse campo alarmaria todo dia, e alarme falso ensina a pessoa a desligar o aviso justamente antes da noite em que ele importaria — o dano que o `alerta_cotas.py` existe para evitar. O dado incoerente **não é apagado**, só marcado (`chuva_coerente` + `chuva_incoerencias`), no mesmo vocabulário do resto da coleta. Seis testes, incluindo que janela ausente **não** conta como zero (buraco não é chuva zero) e um que trava o achado contra a página real: se a fonte se corrigir, o teste cai e a descoberta é revisitada. Achado que destrava o resto: a **régua oficial do rio** — a que a legenda do `/estacao/ver/21` descreve — sai **coerente**, então é nela que o gatilho por chuva vai se apoiar quando a ambiguidade da coluna for resolvida.

- [x] **Duas "correções" recebidas foram CONFERIDAS NA FONTE E RECUSADAS — as duas piorariam o aviso.** Chegaram como documentos de sessão, com ar de resolvido; a fonte primária desmente ambas. **(1) Gaspar 5/6/7 "está errado", trocar por 6/7 sem faixa de alerta.** Não está errado: o `data/brutos/gaspar-plano-de-contingencia.pdf`, item 4.2.3, p. 25, define **quatro** faixas — `0–5 normalidade · 5–6 atenção · 6–7 alerta · >7 resposta` —, `scripts/conferir_gaspar_plano.py` refaz a conferência (as 26 vias do quadro batem ao centavo) e há teste travando `{5.0, 6.0, 7.0}`. A legenda do site é a **vista operacional simplificada** do mesmo documento, não uma segunda verdade. Aplicar moveria a atenção de 5,00 para 6,00 m: o site avisaria **um metro mais tarde**, e a primeira rua alaga a 6,20 m. **(2) Reestruturar o `transito.json` por âncoras + interpolação**, sob a premissa de que "as cidades intermediárias não têm tempo medido". Elas têm: a Tabela 7.5.1 da JICA, gravada no `_meta`, dá valor para **todas** as do tronco, **é monotônica sozinha**, e os seis trechos gravados batem com ela no **máximo**, um a um. A proposta substituiria valor de fonte publicada por interpolação linear nossa — cuja premissa (velocidade constante) o próprio documento admite ser falsa — no horário que o morador lê. Os dois documentos entraram em `docs/` **com um aviso de verificação no topo**, para que uma sessão futura não os "aplique": guardar o documento sem guardar a conferência é como o erro volta.
- [x] **Validador de monotonia da janela de chegada — separando sobreposição de contradição.** A ideia veio do `JANELA-DE-CHEGADA.md` e é boa; a implementação compara o que de fato é **impossível** (`min_montante > max_jusante`, sem atribuição de tempos que satisfaça as duas janelas) e deixa a **sobreposição** como aviso. A diferença não é sutil: hoje a janela de Blumenau começa 3 h antes da de Indaial, que fica acima — mas 10 ≤ 10, então existe mundo consistente, e é o que o hidrograma afirma (as duas na mesma hora). Tratar isso como defeito é o que empurra alguém a trocar dado de fonte por interpolação. A busca de caminho **repete a do site** (`caminho()` do `transito.ts`: direto quando existe, senão busca em largura), porque validar um percurso que a tela não usa aprovaria dado que a tela mostra errado. Quatro testes travam as duas regras, o empate no limite e a paridade com a busca do site.
- [x] **Achados de 03/09 registrados como pendência acionável** (os que a fonte não contradiz): o gatilho **composto** de Gaspar (atenção por nível **ou** chuva > 6 mm — único na bacia, e só acrescenta gatilho), a quebra de **1986** na régua de Gaspar pela retificação do canal (Santos & Pinheiro, 2002 — nunca comparar pico pré e pós), os **dois regimes** da Barragem Oeste (retendo × vertendo, que quebra correlação calibrada num só), o portal de **Taió** não explorado, a exportação das cotas de rua de **Rio do Sul** pelo próprio site, e a **correlação Blumenau↔Gaspar do CEOPS/FURB**, que é a previsão a jusante já feita por quem tem a série completa. Os dois conjuntos de dados mais valiosos — 69 picos históricos e 28 abrigos com cota, de Gaspar — **não puderam ser importados: os arquivos não vieram nos envios** e o site é inalcançável deste ambiente.

### Sessão de 03/09/2026

- [x] **A coordenada de onde NASCE o Itajaí-Açu: `-27.2160314, -49.6483391`, em Rio do Sul.** A topologia canônica já afirmava o fato (as duas cabeceiras se juntam em Rio do Sul, verificado em OSM/Overpass); faltava o **ponto**. Ele saiu do traçado que a Defesa Civil de Rio do Sul publica pela API Asthon — e saiu mais forte do que se esperava: os três traçados (Itajaí do Sul, Itajaí do Oeste e Itajaí-Açu) **terminam/começam no MESMO vértice, ao dígito**, distância 0,0 m. A junção é **declarada pela topologia da fonte**, não inferida por nós medindo aproximação — e o novo `scripts/achar_confluencia_cabeceiras.py` **recusa** justamente por isso se o vértice deixar de ser compartilhado, em vez de devolver "o ponto mais próximo", que seria inventar precisão que a fonte não deu. **Conferência independente que fecha o caso:** o rumo de chegada de cada cabeceira bate com a geografia que o projeto tinha por OUTRA fonte — o Itajaí do Sul chega pelo **sul** (vem de Ituporanga), o Itajaí do Oeste pelo **oeste** (vem de Taió). Rumo trocado também é recusa: significaria arquivo com nomes trocados ou topologia nossa errada, e nenhuma das duas se resolve gravando. Nove testes, quase todos de **recusa** (traçado ausente, vértice solto, rumo errado, traçado gravado ao contrário, idempotência da gravação), mais a checagem de que a longitude encurtada pelo cosseno da latitude não faz um deslocamento norte-sul parecer leste-oeste no paralelo 27. O que **não** mudou: a cobertura do arquivo segue curta demais para virar traçado de mapa (10,6 km no Sul, 9,9 km no Oeste) — continua em `data/brutos/`, e a pendência de completar pelo Overpass segue aberta.
- [x] **Seletor de fundo no mapa de manchas: Escuro (padrão), Satélite e Mapa.** Implementa `docs/CAMADAS-DE-MAPA.md` na tela onde o ganho é maior e o esforço menor — a de Itajaí, que já usa Leaflet. O padrão continua **escuro por função**: qualquer fundo com textura concorre com a mancha, que é o dado da tela. Três pontos que não são detalhe: (1) **pane próprio** (`createPane('fundo')`, zIndex 180) — sem ele a troca exigiria `bringToBack()`, que joga a camada nova para trás das tiles antigas que o Leaflet mantém no DOM, e a tela não muda; (2) a **atribuição vive colada na URL** e troca junto com a camada, inclusive no crédito do rodapé, que antes creditava o OpenStreetMap fixo e ficaria **errado** com o satélite ligado — atribuição é condição de licença das três fontes; (3) sobre satélite o contorno da mancha vai a 1,4 px `#04141f` e o preenchimento a 0,72 de opacidade, porque o azul translúcido de 0,6 px some contra telhado e mata. Trocar de fundo **não rebaixa o GeoJSON** (até 651 kB por evento): repinta com `setStyle`. A escolha fica em `localStorage` dentro de `try/catch`, e a tela abre no padrão quando vier vazio ou bloqueado. **Nada de Google Maps/Earth** — a licença não permite embutir tiles em site próprio. Conferido no navegador a 320 px: as três camadas montam a URL certa (a do Esri com o `{z}/{y}/{x}` invertido, verificado contra a do CARTO no mesmo ponto), a atribuição troca nos dois lugares, o padrão abre em escuro, a preferência sobrevive ao recarregamento, os botões têm `aria-pressed` e foco visível de 2 px por teclado, e o seletor fica **acima** do mapa, sem cobri-lo. **As tiles em si não foram vistas:** os domínios de mapa estão bloqueados no ambiente de desenvolvimento — o que a própria especificação previa —, então o que se conferiu foi a URL, não a imagem.
- [x] **Guia "Monitor": a bacia INTEIRA em tela cheia, alta definição.** Nova aba `/monitor` que desenha **Itajaí-Açu + Itajaí-Mirim juntos** (e os afluentes, quando existirem no pacote) num `<canvas>` só, ocupando a largura toda, com `devicePixelRatio` até 3 e botão de tela cheia. O motor de desenho foi **extraído** para `web/src/logica/mapaMotor.ts` (compartilhado com o mapa de um rio) e `construirCena` passou a aceitar **vários rios**. Camadas, todas ligadas ao dado real (nunca ao instantâneo do `.md` de referência): faixa por régua + correnteza, **mar/maré** na foz, **chuva** recente por cidade (gota azul com mm), **idade** de cada leitura, **legenda** sempre visível. Toque numa cidade abre o cartão + atalho para a tela do rio; acesso por teclado/leitor via botões fora da vista; não é alerta oficial (199 na tela).
- [x] **Onda descendo até o mar + reprodução das 24 h.** Uma **onda** (crista azul-água) varre cada rio da nascente à foz — direção e movimento do "a água desce para o mar", por cima do leito colorido, sem afirmar nível (a faixa segue sendo o sinal; no cinza a crista é fraca). E uma **reprodução (playback 24 h)** no monitor: um play + barra que roda as últimas horas e **recolore o mapa** pela faixa de cada cidade naquele instante (via `serie-recente.json`, a leitura ATÉ o instante — nunca a futura); é o MEDIDO, não previsão; no passado a chuva some e ao fim volta ao vivo. Vale também no mapa de um rio (`/acu`, `/mirim`). `prefers-reduced-motion` congela tudo.
- [x] **Mar na foz, colorido pela maré (escala própria, nunca a de cheia).** Na foz do mapa (Itajaí, a leste), o `<canvas>` passou a desenhar o **MAR**, tingido pela **maré** numa escala **azul separada** das faixas de cheia. É uma distinção de segurança: **maré alta não é cheia** — as réguas do estuário são `alerta_automatico: false` porque a oscilação da maré é maior que a distância até a cota —, o que a maré alta faz é **travar o escoamento** do rio. `estadoMareAgora` (`web/src/logica/mare.ts`, com testes) lê a tábua (`data/mare-itajai.json`) e diz se a maré **sobe ou desce agora** e a que altura do ciclo; o azul segue a altura e um chip mostra "Mar · Maré subindo/baixando". **A tábua está vazia hoje**, então o mar aparece **cinza, "maré: sem dado"** — nada é estimado; acende quando `scripts/coleta_mares.py` preencher a tábua. Vale nas telas `/acu` e `/mirim` (as duas terminam em Itajaí).
- [x] **Mapa do rio virou um `<canvas>` animado (estudo do protótipo Grok, pedido "animação moderna bem intuitiva").** O mapa geográfico do rio deixou o Leaflet e passou a ser desenhado num `<canvas>` próprio: o mesmo traçado do OSM (`data/rios/*.geojson`) pintado por trecho na cor da faixa da cidade **a montante** (a mesma regra do diagrama), agora com a **correnteza animada** descendo no sentido do rio e **mais rápida onde o nível está mais alto** — a animação **significa o nível**, não enfeita. Trecho sem régua que o pinte fica **cinza e parado** (`VEL_FAIXA['sem-dado'] = 0`): não se anima uma água que não se mede. O sentido do fluxo é orientado pela espinha montante→jusante (os ways do OSM não vêm todos na ordem do rio), e a geometria pura ficou isolada e testada em `web/src/logica/mapaCanvas.ts` (projeção, orientação, trecho, movimento das setas). Extras: altura do mapa ajustada à proporção da bacia, **rótulos com anticolisão** (na foz amontoada, o nome da faixa mais grave ganha o espaço; o omitido reaparece no toque), `prefers-reduced-motion` congela o movimento, e botões fora da vista dão acesso por teclado/leitor de tela. **Honestidade preservada:** cor é faixa na régua da cidade, **nunca metro entre cidades**; o aviso de fonte segue na tela. **Ressalva medida:** o **Leaflet continua no pacote** porque o **mapa de manchas** de Itajaí (`MapaManchas`) depende dele (o fundo de ruas é essencial ali) — então a economia da dependência **não se realiza** enquanto aquele mapa não mudar; o ganho real é que `/acu` e `/mirim` **deixaram de baixar o Leaflet** (o chunk do mapa do rio caiu de ~160 kB para ~10 kB).

### Sessão de 01/09/2026 (durante uma cheia real na bacia)

- [x] **Tela do rio no estilo Kikikuru (JMA) — os quatro itens do brief, ver `docs/kikikuru.md`.** O rio deixou de ser pontos soltos e virou linha colorida por trecho, no diagrama linear e num **mapa geográfico** novo (Leaflet + traçado do OpenStreetMap em `data/rios/*.geojson`, ODbL): cada trecho ganha a cor da faixa da cidade **a montante**, encaixando as cidades no traçado por uma espinha montante→jusante. A **cor é ação, não enfeite**: cada faixa traz a frase que remete à Defesa Civil. Entraram também a **linha do tempo de 24 h** por cidade (`LinhaDoTempo`, gráfico da régua com as cotas como faixas, lê `serie-recente.json`) e a **reprodução da onda descendo** (`AnimacaoOnda`: um play que, a cada instante, pinta cada cidade pela faixa **dela** naquele momento — reprodução do que foi MEDIDO, nunca previsão; cinza onde não há leitura fresca). O **zoom troca a informação** (nomes das cidades só de perto; tocar numa cidade abre as cotas de rua e o abrigo dela), e no **desktop** o mapa fica numa coluna fixa à esquerda com os dados rolando à direita — no celular segue coluna única, o mapa sob o botão. A projeção de chegada a jusante já existia (`PainelSePicoAgora`) e foi apontada, não duplicada.
- [x] **Blumenau parou de sumir atrás de "várias réguas" — bug visto NA cheia.** A leitura primária (página de Itajaí, que veio velha) e o resgate do AlertaBlu (fresco) são a **MESMA régua** (ANA 83800002, mesmo zero); o site as tratava como duas, avisava que "não se comparam" e **escondia que Blumenau estava em inundação**. `leituraDaCidade` passou a agrupar por régua (`resgate_de ?? estacao`, o mesmo critério do vigia): primária + resgate colam numa régua só e vale a mais fresca. Só devolve "sem nível único" quando há réguas **distintas de verdade** (Itajaí, onze zeros diferentes). Quatro testes travam os dois lados.
- [x] **`data/faixas.json`: fonte única dos textos das faixas — e o site deixou de recomendar ação.** Os textos de faixa saíram do código para um JSON que o site lê (não podem mais divergir do canon). E aplicou-se o rigor da regra de responsabilidade: o site **descreve** a faixa e **remete** ("Siga a Defesa Civil", "ligue 199"), nunca recomenda ação. Caíram "Prepare o que levar", "procure lugar seguro" e "Fique atento e confira a cota da sua rua". Varredura de toda a interface: nenhuma outra recomendação sobrou (o que parecia — "Procure a sua rua" é rótulo de busca; "não espere… para sair de área de risco" é disclaimer — remete ao uso da tela ou à autoridade).
- [x] **Série das últimas 48 h publicada (`serie-recente.json`).** O navegador só tinha o `ultimo.json` (um instante); a série acumulada em ndjson é matéria-prima gitignorada, só na VPS. Agora `coleta_niveis.py` recorta as últimas horas de **nível** por rio e cidade e `publicar_tempo_real.sh` leva o arquivo junto do `ultimo.json` no branch `tempo-real`. É a base do slider e da animação. Só nível (chuva é outra grandeza); `medido_em` em hora de Brasília, sem fuso.
- [x] **Vidal Ramos com nível ao vivo (Asthon) — 11ª cidade coberta.** A cidade era uma das dez sem nível. `scripts/coleta_asthon.py` pega Vidal Ramos por **lista fechada de `station_id`** (barragem, altitude e cota copiada de Rio do Sul ficam de fora, como `analisar_asthon.py` já dizia), converte o `last_reading_at` de **UTC → Brasília sem fuso** na entrada, e é fiado em `coleta_niveis.py` no molde do resgate do Blumenau (falha nunca derruba a coleta). Confirmado ponta a ponta na VPS. **Sem cota de referência ainda**, então mostra o nível com a idade à vista e a faixa fica **cinza** — mostra, nunca dispara.
- [x] **Classificação da estação estadual de Gaspar (`DCSC-00005`) precisada.** A metadados do GraphQL estadual **declara** sensor de rio (`rio_nivel.value=true`), então não é pluviômetro puro; mas o **valor** de nível veio nulo em 3/3 observações — na prática publica só chuva. Os docs deixaram de dizer só "publica chuva, não régua" (que negava o sensor) e passaram a dizer as duas metades; a estação **não** entra em `estacoes.json`, porque registrar uma régua que nunca devolveu número seria cobertura aparente.

- [x] Scaffold do site (React + Vite + TypeScript strict) com as quatro telas e leitura direta dos JSONs de `data/`.
- [x] Diagrama linear do rio com cotas, fontes de tempo real e tempo de trânsito entre cidades.
- [x] Gráfico de picos históricos por cidade, com cor por confiança e tabela de fontes.
- [x] Lógica de previsão empírica com as travas de segurança descritas acima, coberta por 22 testes.
- [x] `scripts/validar_dados.py` e CI rodando validação, testes e build a cada push.
- [x] **Token do Telegram não vaza mais para o log.** Eram três caminhos, todos reproduzidos antes do conserto: o `erro na rodada` do laço principal, o aviso de falha ao registrar o menu, e — o pior — `--uma-vez`, que deixava o traceback do Python subir com a URL inteira. O token viaja no caminho da URL (`/bot<token>/getUpdates`), então qualquer erro de rede o carrega. Agora tudo passa por `notificador.sem_segredo()`, que apaga o token configurado **e** o que estiver entre `/bot` e a próxima barra — o que protege também log antigo, de antes de uma troca de token. 19 testes novos, incluindo uma guarda que lê o fonte e cobra a limpeza em todo `print` e `return` que leve um erro para fora.
- [x] **As três camadas do ArcGIS de Itajaí baixadas e analisadas.** `scripts/baixar_itajai_arcgis.py` trouxe do serviço público da Prefeitura, sem navegador, as 10 camadas de inundação, os 5.237 pontos cotados e os 110 polígonos de terreno. As contagens bateram com o previsto até nos detalhes. A análise devolveu **dois "não" e um "sim"**: não trocar as nossas manchas pelas do ArcGIS (mesma geometria, atributo derivado a mais, e perderíamos a licença MIT declarada); não mostrar o terreno sujeito a inundação (38,7 ha contra 7.086 ha da mancha de 1983 — 183 vezes menos que o nome promete); e sim à área atingida por evento, que três camadas publicam e confere com a geometria: 1983 = 7.086 ha, 1984 = 7.015 ha, 2001 = 3.425 ha.
- [x] **Tocar num ponto do mapa de Itajaí diz em quais enchentes ele ficou dentro da área atingida.** É a metade do "meu ponto" que não depende de referência altimétrica nenhuma — um fato sobre polígonos que já estavam no repositório, verdadeiro ou falso, não estimado. A outra metade (responder em metros) continua bloqueada, e o porquê está em `docs/tela-itajai.md`. "Nenhuma mancha" não vira "não alaga": a resposta negativa sai com a ressalva de que o levantamento cobre o que foi mapeado. Dez testes na lógica pura, incluindo o buraco de polígono e o vértice na altura exata do ponto.
- [x] **As cotas de Blumenau conferidas contra o PDF oficial — e o abrigo de cada rua.** Os 1.938 pontos de Blumenau tinham entrado por imprensa, em `confianca: media`. O PDF da Secretaria Municipal de Defesa Civil (111 páginas, 2.034 registros) chegou, e o cruzamento pelo mesmo ponto de cada rua deu **1.891 de 1.891 batendo ao centavo, zero divergências, deslocamento mediano 0,00 m**. As conferidas subiram para `alta`; **nenhum número mudou** — a operação confirma, não corrige. O PDF ainda traz o que a imprensa não trazia: **o abrigo de cada rua**, com código, que chegou a 2.018 dos 2.042 registros e agora sai no site e no bot logo abaixo da cota. A cota diz que é hora de sair; o abrigo diz para onde. Mais 104 pontos que só o PDF tem entraram. Blumenau: 1.938 → 2.042.
- [x] **Cotas de rua de Gaspar: 23 → 1.618, com `confianca: alta`.** O KML da Defesa Civil de Gaspar chegou com a mesma armadilha do de Brusque — uma conversão pronta que já gravava tudo como `referencia: "régua"`, sem evidência nenhuma. `scripts/analisar_kml_gaspar.py` testou a afirmação com o mesmo instrumento que recusou a camada de 2011 de Brusque, e desta vez ela passou por dois caminhos independentes: as **4 ruas em comum com o nosso cadastro batem todas ao centavo, e sempre no menor valor da rua** (em Brusque eram 4 de 13, com nove divergindo até 2,3 m), e as **duas listas que o estudo do CEOPS publicou saem na ordem certa e separadas** (medianas 6,63 m e 7,07 m, P por acaso = 0,0014), sem que nada no arquivo diga a que grupo cada rua pertence. O importador refaz a análise antes de gravar e recusa se o veredito mudar. Dezoito registros sem número foram substituídos pelas cotas oficiais; os cinco com número ficaram, porque são eles a prova de escala. Na época Gaspar seguia **sem cota em `estacoes.json`**, e os números respondiam "a partir de quanto minha rua alaga" sem disparar aviso; as faixas da régua chegaram depois, pelo Plano de Contingência (ver o item das cotas de régua de Gaspar, em Pendências).
- [x] **Cotas de rua de Brusque: 27 → 377, com `confianca: alta`.** O KML original do My Maps chegou e resolveu o que a conversão anterior tinha perdido: cada marcador da camada de 2023 traz o **nome** (a cota da régua) e a **lâmina d'água** medida em 17/11/2023, e os dois somam **8,96 m** — o pico daquele dia — em **338 dos 344 pontos** que publicam lâmina, ao centímetro. Não é inferência sobre a fonte, é aritmética contra um pico conhecido, e `scripts/importar_cotas_brusque.py` a refaz a cada execução e **recusa a importação inteira** se ela deixar de fechar. Seis pontos ficaram de fora por a própria conta não fechar — quando os dois números da fonte discordam não dá para saber qual está errado. A camada de 2011 segue recusada, e `scripts/teste_analisar_kml_brusque.py` foi **apertado**, não afrouxado. Achado junto: um ponto da Av. Beira Rio alaga a 3,76 m, 1,04 m antes da cota de atenção da cidade.
- [x] **Lista de Rio do Sul completa e conferida contra segunda fonte.** Duas leituras independentes da tabela oficial — a nossa, do portal (554 ruas, `alta`), e a transcrição da NSC Total (545 ruas, `media`) — batem em **538 de 538 ruas em comum, ao centavo, com zero divergências**. A conferência achou a rua que faltava: **Visconde de Cairu, 19,01 m**, que não é grafia de nenhuma outra. Rio do Sul fecha em **555**, que é o total que o portal declara. Refazível com `scripts/conferir_rio_do_sul_nsc.py` (16 testes).
- [x] Série histórica do relatório documental incorporada: 116 registros (97 de Blumenau desde 1852, 9 de Rio do Sul, 8 de Brusque, Taió e Timbó), cada um com fonte, confiança e divergências.
- [x] Eixo do Itajaí-Açu completo em `transito.json` a partir do estudo JICA, incluindo Ituporanga, Apiúna e os trechos entre Blumenau e a foz.
- [x] Painel de maré na tela da foz, com coletor da tábua oficial e cálculo de sizígia.
- [x] Nível ao vivo no site, com selo de idade e recusa de calcular chegada a partir de leitura velha.
- [x] Site publicado no GitHub Pages, com os dados validados antes de cada publicação.
- [x] **554 cotas por rua de Rio do Sul**, da tabela oficial da Defesa Civil — dez vezes tudo que havia. A tabela viaja dentro do pacote da aplicação do portal; o importador (`scripts/importar_cotas_rio_do_sul.py`) confere o `robots.txt` antes, mescla sem tocar em outra cidade e é idempotente. Duas cotas que a fonte publica abaixo do nível normal do rio entraram com `usar_para_aviso: false` — aparecem na busca com a ressalva e não movem alarme, pelo mesmo motivo das réguas de estuário de Itajaí. A tabela saiu do pacote inicial do site: com dez vezes mais dado, ele ficou menor (310 → 285 kB).
- [x] Cotas oficiais das onze réguas de Itajaí e da de Ilhota (Plano de Contingência da COMPDEC, v17), com as nove de estuário travadas contra aviso automático — e **visíveis na tela e no bot**: antes o site dizia "cotas de referência não levantadas" e o `/cotas` dizia "ainda não foram levantadas" para duas cidades cujas cotas estão publicadas. Os ribeirões da Murta e da Canhanduba aparecem na tela da foz; no painel `/rios` do bot cada um entra no rio em que deságua — **Murta no Itajaí-Açu, Canhanduba no Itajaí-Mirim** (conhecimento local, 02/09/2026; a confirmar no mapa de bacia do Plano da COMPDEC). Seguem réguas de estuário, sem aviso automático.
- [x] Registro das 14 estações de tempo real com o título exato da fonte, pronto para receber a cota de cada régua.
- [x] Bot de consulta no Telegram (`/nivel`, `/chuva`, `/previsao`, `/cotas`, `/rios`), com o encadeamento de trânsito amarrado ao do site por um gabarito compartilhado.
- [x] Chuva acumulada por cidade (1 h / 12 h / 24 h / 48 h), agregando os pluviômetros e recusando leitura que não fecha.
- [x] Aviso por Telegram quando um rio cruza cota, e vigia que percebe a coleta morrendo — ver **Avisos** abaixo.
- [x] Caminho completo para registrar cheias novas: coleta acumulada em formato enxuto, extração de picos com data e hora, e calibração dos tempos de descida a partir deles.
