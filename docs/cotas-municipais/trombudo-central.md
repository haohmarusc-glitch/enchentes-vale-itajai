# Trombudo Central — Rio Trombudo

**Pico observado:** **8,71 m** (nov/2023) — “maior enchente da história” (prefeitura + GOVSC).  
**Obras:** R$ 6 mi / 20 km (Trombudo + Braço do Trombudo).

Não há tabela atenção/alerta. O 8,71 m entra no JSON só como `inundacao_historica` / pico, com `verificado: false`.

```json
"cotas_m": {
  "inundacao_historica": 8.71
}
```

Barra do Trombudo em **Rio do Sul** é outro lugar — não misturar.
